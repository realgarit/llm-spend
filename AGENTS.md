# Repository instructions

> Canonical instructions for all coding agents (Claude Code, Codex, GitHub Copilot). Claude loads this via the CLAUDE.md stub.

## Changelog requirement

For every customer-visible change to model availability, provider coverage, pricing, cache rates, calculations, or comparisons, update `src/data/changelog.ts` in the same pull request.

Before completing the work, confirm that the changelog entry states the customer-visible effect and uses the current date. Do not add an entry for internal-only refactors, test-only changes, or tooling changes that do not alter the site’s displayed behavior or data.

## Git Workflow: Always PR

Always ship changes via branch → commit → push → PR → CI → merge → delete branches; never commit directly to main.

**Why:** No direct commits to main; CI must validate before merge, and stale branches must not accumulate.

**How to apply:** For any completed change: create a feature branch, commit, push, open a GitHub PR, wait for CI to pass, merge the PR, then delete both the local and remote feature branches and update local main. Do this without asking each time.

## Cross-agent conventions

- This file (`AGENTS.md`) is the single source of truth for agent instructions in this repo. `CLAUDE.md` and `.github/copilot-instructions.md` are pointers to it — never edit them, never duplicate content into them.
- Reusable skills live in `.claude/skills/` (one folder per skill with a `SKILL.md`). GitHub Copilot reads that directory natively; Codex sees it via the `.agents/skills` symlink. New skills always go in `.claude/skills/`.
- Claude-specific subagent definitions live in `.claude/agents/`. If you are not Claude Code, you may read them as role/process guidance.
- Session continuity across tools: before ending substantial work in ANY tool (Claude Code, Codex, Copilot), record durable context — decisions made, gotchas discovered, in-progress state worth resuming — in the "Working notes" section below, or fold it into the relevant section above. This is the shared memory between agents.

## Working notes

<!-- Any agent: append short dated notes here (YYYY-MM-DD — note). Prune notes when stale or once folded into the sections above. -->

- 2026-07-20 — Gotcha: Alibaba's Model Studio pricing page lists model families beyond the obvious names — the 2026-07-19 Qwen capture missed the qwen3.7 flagships (snapshots dated May 2026) by only sweeping "3.6" rows. When capturing that page, grep table rows for ALL `qwen` matches, not the family you expect. Its price columns are Input | Output (non-thinking) | Output (thinking), and "List price $X Limited-time N% off" means the shown figure is BEFORE discount.
- 2026-07-20 — Watch: Qwen3.7 Max (50% off) and Plus (20% off) are promo rates with no published end date; if the promo lapses, catalog reverts to list ($2.50/$7.50 and $0.40/$1.60 ≤256K). Still pending elsewhere: Grok 4.5 on Foundry, non-Global Foundry increase 2026-09-01, Sonnet 5 intro ends 2026-08-31. (GPT-5.6 on Azure: resolved 2026-07-21, see below.)
- 2026-07-21 — Gotcha/tool: Azure's Foundry pricing page (`/pricing/details/ai-foundry-models/...`) would not render its serverless tables in the in-app browser (only 4 Managed Compute GPU rows appear; screenshots time out). Reliable official alternative: the Azure Retail Prices API — `https://prices.azure.com/api/retail/prices?$filter=serviceName eq 'Foundry Models' and contains(tolower(meterName),'<term>')` (URL-encode `$filter`; plain curl works). Meter-name decoder: ShortCo/LongCo = short/long context, Inp/Opt = input/output, Cd Inp/Cd Wr = cached input/cache write, Std/PP = standard/priority processing (PP ≈ 2x), Gl/DZ = Global/DataZone (DZ = 1.1x). Prices are per the `unitOfMeasure` field (GPT-5.6 meters are per 1M; older Grok meters are per 1K).
- 2026-07-21 — Resolved: GPT-5.6 Sol/Terra/Luna now have official Foundry meters (effective 2026-07-01) at exactly the OpenAI direct rates; catalog upgraded to official via PR #16. Confirmed via the same API sweep: no Grok 4.5 meter, no Qwen serverless meter (only Qwen3 32B FT hosting). Oddity noted, not catalog-affecting: the retail API's Grok meter names lag Foundry marketing names (a "Grok 4.2" meter carries the $1.25/$2.50 price the pricing page shows for Grok-4.3).
- 2026-07-21 — Changelog provenance convention: every `ChangelogEntry` has a non-empty `sources` array of `{ label, href }`; the page renders these as the final footer beneath each entry. Prefer official launch posts, pricing docs, and Azure Retail Prices API queries. `src/data/changelog.test.ts` enforces coverage and URL shape for the full history.
- 2026-07-21 — Grok 4.5's official model page now publishes cached input at $0.30/M; the earlier $0.50/M third-party estimate was corrected and upgraded to official provenance.
- 2026-07-22 — Resolved/observed: Azure published dedicated "FW GLM 5.2" meters effective 2026-07-01 — input/output equal to GLM 5.1 DZ but cached input cut to $0.00015/1K ($0.15/M, ~48% below 5.1); catalog upgraded to official. Also new "FW Kimi K2.7 Code" DZ meters (2026-07-01) at exactly 1.1x the catalog's Global rates — consistent, no catalog change.
- 2026-07-22 — Gotcha: GPT-5.6 retail meters are named "5.6 sol/terra/luna ..." — a meterName search for "gpt" misses them entirely; search "5.6", "sol", "terra", or "luna" instead.
- 2026-07-22 — Scope note: Google's pricing page also lists Gemini 3.5 Flash-Lite ($0.30/$2.50, cached $0.03) and Gemini 3.5 Flash Cyber alongside 3.6 Flash. Flash-Lite is deliberately left out of the catalog to keep a single focused Gemini lane; Flash Cyber has no published pricing at all, so it can't be added yet. Gemini cache storage ($1.00 per 1M tokens per hour) is a billing dimension this site's schema doesn't model — recorded in each entry's `sourceNote` only.
- 2026-07-23 — Resolved: Foundry cached-input meters published for K2.5 ($0.10/M, 'K2.5 cached glbl' $0.0001/1K) and K2.6 ($0.16/M, 'K2.6 cached glbl' $0.00016/1K — replaces the old $0.19 hidden-meter derivation), both eff. 2026-07-01; and Grok 4.3 ($0.20/M, meter named bare "4.3" — a 'grok' meterName search MISSES it — 'Cached Inp Glbl' $0.0002/1K, eff. 2026-05-01, long-context "L" variant 2x at $0.40/M). Watch: the US-Gov regions carry slightly higher cached prices (K2.6 $0.20, K2.5 $0.000125/1K) — use the commercial `isPrimaryMeterRegion` majority price, not the gov outlier. Also new non-FW "K2.7 Code" Global meters ($0.95/$4.00 with cached glbl $0.19) match catalog — no change; oddity: its non-FW DZ output meter is $5.00/M, disagreeing with "FW Kimi K2.7 Code Opt DZ" at $4.40/M.
- 2026-07-23 — Gotcha: Mistral meters are named "MM3.5" / "OCR 4" — a 'mistral' meterName search finds NOTHING. Mistral Medium 3.5 landed on Foundry (Global $1.50/$7.50 = direct-API parity; DataZone $1.65/$8.25 = 1.1x; NO cached meter on any tier), eff. 2026-06-01 (DZ) / 2026-07-01 (Global). NOTE: the DataZone meters DO exist — an earlier handoff wrongly assumed "no DZ meter"; only the cached meter is genuinely absent. OCR 4 is per-page ($4/1K pages), out of schema scope.
- 2026-07-23 — Gotcha/watch: GPT-5.6 Terra ShortCo Std DZ input/output meters are SWAPPED in the retail API ("5.6 terra ShortCo Inp Std DZ" $16.50/M, "Opt Std DZ" $2.75/M; Global is $2.50 in / $15.00 out, so DZ should be $2.75 in / $16.50 out). Obvious data-entry error — do NOT copy into catalog; watch for a fix.
- 2026-07-25 — Resolved/gotcha: GLM-5 (base) was never in the catalog — not pruned, simply never added (`git log -S` confirms no add-then-remove). It is still GA on Z.ai and is the family's cheapest lane, so it was a genuine coverage gap, now filled. Foundry `FW GLM 5` DZ meters (eff. 2026-06-01) are exactly 1.1x the Z.ai direct rates on all three dimensions ($1.00/$1.10 in, $0.20/$0.22 cached, $3.20/$3.52 out) — a useful cross-check pattern. No Global meter for any GLM version; DZ is the only Foundry tier for this family.
- 2026-07-25 — Gotcha (research method): the Z.ai pricing table has FOUR columns — Model | Input | Cached Input | Cached Input Storage | Output. A subagent misread it by one column and reported GLM-5 input as $0.20 (the cached value) with cache "Limited-time Free". Correct row: input $1, cached input $0.2, storage "Limited-time Free", output $3.2. "Limited-time Free" refers ONLY to cache STORAGE (a per-hour dimension out of schema scope), never to the per-token rates. Always transcribe that table column-by-column, and sanity-check the output/input ratio (~3.2x across the GLM family) before trusting a parse.
- 2026-07-25 — Watch/inconsistency: the GLM "Direct" rows for 5.1 and 5.2 are labelled "Fireworks direct API" with `cachedUsd: null`, but Z.ai (the actual first-party API per `types.ts`, which defines Direct as the developer's own API) publishes $0.26/M cached for both. The new GLM-5 direct row is sourced from Z.ai and does carry its cache rate, so the family's Direct lane is currently mixed-vendor. Left unchanged deliberately — resolving it means deciding whether GLM's Direct lane should be Z.ai or Fireworks, which is a scope call, not a data fix.
- 2026-07-25 — Open/unverified: Foundry publishes separate `K2.6 Thinking` meters (Glbl $0.95/$4.00, DZ $3.00/$5.00) with no matching catalog row. A prior check called this a non-gap on the basis that Moonshot bills thinking as a parameter rather than a separate SKU — but that rested on a third-party source (TokenMix), NOT platform.kimi.ai directly. Treat as unresolved; verify against Moonshot's official pricing page before adding or dismissing.
- 2026-07-23 — Scope: Cohere "Command A Plus" (Global $0.80/$3.20 per M) and gpt-realtime-2.1(-mini) landed on Foundry eff. 2026-07-01 — deliberately excluded (Command A Plus is outside the tracked lanes; realtime/audio and per-page OCR are not modeled by the schema).
- 2026-07-23 — Watch: Qwen3.7 Max (50% off) and Plus (20% off) promos re-verified LIVE on Model Studio's pricing page 2026-07-23 — still discounted, no end date; third-party "promo ended" claims are false. Could NOT confirm any qwen3.8-max / qwen3.8-max-preview listing on either the pricing or models page (searched 2026-07-23) — not added; revisit if it appears with a per-token price. Grok 4.5 still has no Foundry meter (re-verified 2026-07-23; retail catalog tops out at the "Grok 4.2/4.3" meters). DeepSeek deprecates the `deepseek-chat`/`deepseek-reasoner` model NAMES on 2026-07-24 (pricing unchanged); neither name is referenced anywhere in `src/`, so the catalog is unaffected.
- 2026-07-25 — Resolved: Claude Opus 5 added, GA per Anthropic's official pricing page (platform.claude.com/docs/en/about-claude/pricing) — prices identically to Opus 4.8 ($5/$0.50 cached/$25 per M, 1M context, same newer tokenizer). Separately, the K2.6 Thinking watch item (2026-07-25 entry above) is now RESOLVED: Moonshot's official page (platform.kimi.ai/docs/pricing/chat-k26) confirms K2.6 has a single rate ($0.95 in / $0.16 cached / $4.00 out) with no separate thinking-mode SKU — closes the item that had previously only been checked against a third-party source (TokenMix). No catalog row was needed since Foundry's "K2.6 Thinking" meter price matches the existing tracked K2.6 row exactly.
- 2026-07-26 — RESOLVED: the 2026-07-23 GPT-5.6 Terra ShortCo Std DZ input/output swap watch item is fixed upstream — the Azure Retail Prices API now shows the expected $2.75 in / $16.50 out for that meter, matching the Global rate's 1.1x Data Zone premium. Close that watch item; no catalog change needed since the swapped figures were never copied in.
- 2026-07-26 — RESOLVED: the 2026-07-25 GLM Direct-lane mixed-vendor inconsistency (watch item above) is closed. GLM-5.1 and GLM-5.2's Direct rows are now labelled "Z.ai direct API" (matching GLM-5) with `cachedUsd: 0.26`, sourced from docs.z.ai/guides/overview/pricing. Z.ai is now the family's uniform Direct-lane vendor.
- 2026-07-26 — Confirmed still absent from Foundry as of this run (full `serviceName eq 'Foundry Models'` sweep): Grok 4.5, any Qwen serverless per-token meter, and any Claude/Anthropic meter — so the Claude Sonnet 5 Foundry rows must stay `estimate`. No meter has an `effectiveStartDate` ≥ 2026-08-01, so the rumored 2026-09-01 non-Global price increase is not yet staged.
- 2026-07-26 — Scope decision: GPT-5.4 has a full Foundry meter set (base/mini/nano/pro/LongCo, eff. 2026-03-01) but was DELIBERATELY pruned from the catalog in commit a09afa9 (2026-07-17) — do not re-add it as a "gap" on sight; run `git log -S` on a model name before treating any absence as an oversight.
- 2026-07-26 — New provider added: MiniMax (M2.5 and MiniMax 3), Foundry Data Zone-only (no Global meter), Fireworks-hosted, eff. 2026-06-01 — same shape as the GLM family. Also added: Grok-4.3 Data Zone (1.1x Global, exact), GPT-5.6 Terra/Luna Long Context Global rows (previously only mentioned in a sibling row's `notes`), and Qwen3.7 Flash direct (32K-256K band, matched to the existing Qwen3.6 Flash row's band for comparability — the full ladder is 0-32K/32K-256K/256K-1M).
- 2026-07-27 — Gotcha/major: Foundry Data Zone is NOT one price for Microsoft's first-party OpenAI lines. APAC DZ regions (australiaeast, centralindia, eastasia, japaneast, japanwest, jioindiawest, koreacentral, southeastasia, southindia) are exactly 1.20x Global; US/EU DZ regions are exactly 1.10x. Disjoint region sets, APAC rows eff. 2026-06-01. Affects GPT-5.1/5.2/5.4, the codex variant of 5.3, o4-mini, and text-embedding-3-large/small; NOT GPT-5.3 chat and NOT GPT-5.6. No third-party family (Grok/Kimi/GLM/MiniMax/Mistral/DeepSeek) is affected. When reading a DZ meter, always group by armRegionName — a single retailPrice read is now unsafe.
- 2026-07-27 — Gotcha: DeepSeek V4 Pro has TWO Foundry Data Zone lanes in different products — native `Azure Deepseek Models` (meters `V4 Pro Inp/cached/Outp DZ Tokens`, $1.91/$0.16/$3.83) and `Azure Fireworks Models` (`FW DeepSeek-V4-Pro ... DZ Tokens`, $1.925/$0.165/$3.828). Diffing the catalog's Fireworks-labelled row against the native meter produces a false discrepancy — always check productName before concluding a rate is wrong. Native DZ lane added to the catalog this run.
- 2026-07-27 — Re-verified absent from Foundry (full 29k-row sweep): Grok 4.5, any Qwen serverless per-token meter (only Qwen3 32B FT hosting exists), any Claude/Anthropic meter, and any GPT newer than 5.6. Sonnet 5 Foundry rows stay `estimate`. Zero meters carry an effectiveStartDate >= 2026-08-01, so the anticipated 2026-09-01 non-Global increase is still not staged.
- 2026-07-27 — Watch: `MAI Models` product carries 12 cryptically-named first-party text meters (`MAI o` through `MAI z`, eff. 2026-06-01, $1.75/M to $51.70/M) with no Inp/Outp qualifier and no mappable model identity. Likely a pre-announcement Microsoft first-party family — revisit when officially revealed.
- 2026-07-27 — Scope (untracked, deliberate): the sweep also found serverless per-token meters for Cohere Command A base (Global $2.50/$10.00 — distinct from the already-excluded Command A Plus), GPT-OSS-120B (Global $0.15/$0.60, plus an `FW GPT OSS 120B` DZ lane), Meta Llama 3.3 70B and Llama 4 Maverick, Microsoft Phi, and the older OpenAI o-series (o4-mini Global $1.10/$0.275/$4.40). All left out to keep the tracked lanes focused; do not treat their absence as an oversight.
- 2026-07-27 — Watch/unconfirmed: third-party outlets (TechNode, KuCoin, BigGo) report DeepSeek V4 introduced peak/off-peak surcharge pricing (2x during CN business hours). NOT present on api-docs.deepseek.com's international USD pricing page or its changelog (which stops at 2026-04-24). Either domestic-CNY-only or inaccurate — no catalog change made; recheck.
- 2026-07-27 — Re-verified: qwen3.8-max was previewed at WAIC 2026-07-19 but still has NO per-token price on Model Studio's pricing page (page timestamped 2026-07-25; grep for "qwen3.8" returns 0) — Token Plan credits only. Qwen3.7 Max 50%-off and Plus 20%-off promos still live. Claude Sonnet 5 intro pricing still shown through 2026-08-31.
- 2026-07-28 — RESOLVED/negative: a sweep agent reported a "Grok 4.4" meter set — FALSE. Verified absent three ways (regex over a fresh 29,394-row sweep, a dedicated `productName eq 'Azure Grok Models'` pull = 1,404 rows / 47 meters topping out at 4.3, and a server-side `contains(meterName,'4.4')` query = 0 items). xAI's own docs (docs.x.ai/developers/pricing) confirm the lineup goes 4.3 → 4.5 with no 4.4. Do not re-investigate on a bare sweep claim.
- 2026-07-28 — Gotcha (query artifact): searching the retail API by `serviceName eq 'Azure Deepseek Models'` returns ZERO — that string is a **productName**, not a serviceName. Every model meter lives under `serviceName eq 'Foundry Models'`; the 22 product families are distinguished by `productName`. A serviceName query on a product name yields a false negative, which is how an earlier run wrongly concluded the native DeepSeek meters had vanished. They exist (1,362 rows) and still read $1.91/$0.16/$3.83 per 1M for V4 Pro DZ.
- 2026-07-28 — Corrected: Mistral MM3.5's Foundry **Global** meter is effective 2026-06-01 (44 regions incl. all 9 APAC), not 2026-07-01 as the 2026-07-23 note and the shipped changelog entry state — the 2026-07-01 Global row is just `malaysiawest` onboarding. The live providers.ts sourceNote is fixed; the historical changelog entry was deliberately NOT rewritten (it records what was published then).
- 2026-07-28 — Re-confirmed absent: no Mistral cache meter exists on ANY tier (filtering all 43 Mistral meterNames for `cach`/`Cd ` returns an empty set), so the catalog's `cachedUsd: null` on both Foundry Mistral rows is correct, not a gap.
- 2026-07-28 — Watch (second reason to group by region): the Fireworks DeepSeek `northeurope` region, onboarded 2026-07-01, carries ROUNDED prices ($1.93/$0.17/$3.83) differing from the 21-region majority ($1.925/$0.165/$3.828) in the 3rd-4th decimal. Region grouping is now unsafe to skip for non-APAC reasons too.
- 2026-07-28 — Non-leak note: Grok 4.3 meters carry `armSkuName` values like "Grok 6 Inp regnl" / "Grok 9 Inp DZone". That is an internal SKU counter, NOT evidence of an unreleased Grok 6 — meterName, skuName, and price all say 4.3. Do not misread it in a future sweep.
- 2026-07-28 — Re-verified unchanged on official pages this run: Grok 4.5 direct $2.00/$0.30/$6.00; Claude Sonnet 5 intro $2/$0.20/$10 through 2026-08-31 then $3/$0.30/$15; Kimi K3 $3.00/$0.30/$15.00; GLM 5/5.1/5.2 on Z.ai; DeepSeek V4 Pro/Flash direct USD (still no peak/off-peak surcharge on the international page); Qwen3.7 Max 50%-off and Plus 20%-off promos still live and qwen3.8-max still has no per-token price; OpenAI tops out at GPT-5.6; no GLM 5.3, no K2.8.
- 2026-07-29 — Gotcha/correction: the 2026-07-25 note quoted Foundry's native `K2.6 Thinking` DZ meter as $3.00/$5.00. The $3.00 input is a 3-region outlier (northeurope, switzerlandnorth, switzerlandwest, eff. 2026-07-01); the 16-region majority is $1.045/M (eff. 2026-05-01) — 2.87x apart in EU regions that should be the standard 1.10x lane. Looks like a Microsoft data-entry error of the same shape as the since-fixed GPT-5.6 Terra swap. No catalog impact (the tracked K2.6 DataZone row is the Fireworks lane at 1.045/0.176/4.40), but recheck.
- 2026-07-29 — Gotcha (sweep trap): TWO meter families exist per older GPT version and they differ in APAC coverage. `GPT 5.2 *` / `GPT 5.1 *` carry the 9 APAC Data Zone regions at the 1.20x premium; `5.2 chat 0210 *` / `GPT 5.1 chat *` are priced identically in US/EU but have NO APAC rows at all. Reading the wrong family makes it look like the APAC premium was withdrawn.
- 2026-07-29 — Resolved: Cohere DID delist per-token Embed pricing from cohere.com/pricing, between 2025-07-16 and 2025-08-09 (Internet Archive: present in the 2025-07-16 snapshot at "$0.12 / 1M tokens", gone by 2025-08-09; still gone in every snapshot through 2026-07-24). The site now sells Embed via Model Vault dedicated instances. Foundry DOES have Cohere embed meters — `Embed v4 Txt Glbl` $0.12/M (39 regions, eff. 2026-02-01) and `Embed v4 Txt DZ` $0.132/M (21 regions, eff. 2025-11-01), plus Img meters at $0.47/$0.517 — so Azure is now the only published source for the $0.12 figure. Catalog Direct row downgraded to `derived`; Foundry lanes added.
- 2026-07-29 — Watch: MiniMax labels MiniMax M3's direct rates "Permanent 50% off" with the list price struck through at exactly 2x and NO end date — a promo-shaped label on what is presented as the standing rate. Foundry's `FW MiniMax 3` DZ meters are 1.1x the DISCOUNTED rate, so a lapse would move both lanes. Also: M2.5/M2.1/M2 are now under a collapsed "Legacy Models" heading on MiniMax's page; M2.7 and M3 are current, and M2.7 has no Foundry meter on any tier.
- 2026-07-29 — Scope: GLM-5-Turbo ($1.20/$0.24/$4.00, 200K ctx, agentic-optimized) was NEVER added, not pruned (`git log -S` = 0 commits) — genuine gap, now filled as a Z.ai Direct row. The full Z.ai table also lists GLM-4.7/4.6/4.5 families and free Flash tiers, deliberately untracked to keep the GLM lane focused on the 5.x generation.
- 2026-07-29 — Re-verified unchanged this run (full 29,394-row Foundry sweep + official pages): all ~43 tracked Foundry rows match their live meters exactly; ZERO meters with effectiveStartDate >= 2026-08-01 (the rumored 2026-09-01 non-Global increase is still not staged); still absent from Foundry — Grok 4.4/4.5, any Qwen serverless per-token meter, any Claude/Anthropic meter, any GPT newer than 5.6, any Mistral cache meter, GLM 5.3, Kimi K2.8/K3. Direct pages unchanged for Kimi K3, DeepSeek V4 Pro/Flash (still no peak/off-peak surcharge on the international USD page), Claude (Sonnet 5 intro still ends 2026-08-31), Grok 4.5, Qwen (3.7 Max 50%-off and Plus 20%-off promos still live; qwen3.8-max still has NO per-token price), Mistral, Gemini. Informational: Moonshot announced a "V1 platform sunset" for August 31 — no catalog row references Moonshot V1.
- 2026-07-30 — Gotcha (research method, high value): WebFetch is unreliable on Alibaba Model Studio's pricing page and produced FOUR consecutive false negatives for `qwen3.7-flash` — a string present 3x on the live page. Two compounding causes: the page is ~72KB of innerText across 234 tables (summarizer truncation), and price bands live in `rowspan` continuation rows that carry no model name, so band data is textually detached from the model it belongs to. Always check that page with a real browser DOM query (`preview_start` then `javascript_tool`), walking cells with `rowSpan` rather than reading `tr.innerText`. A fetch-and-summarize negative on this page is NOT evidence of delisting — it nearly caused a correct row to be deleted.
- 2026-07-30 — Verified present, no change: the qwen3.7-flash International ladder is 0–32K $0.030/$0.130, 32K–256K $0.100/$0.400, 256K–1M $0.200/$0.800 (alias resolves to qwen3.7-flash-2026-07-15); the catalog row matches exactly. Note the page also publishes a SEPARATE `Global` deployment-scope table at different, lower rates ($0.028/$0.110 at ≤32K) — always confirm the Deployment scope column reads "International" before transcribing.
- 2026-07-30 — Correction to the 2026-07-27 APAC note: GPT-5.5 is ALSO in the APAC 1.20x Data Zone set (`5.5 ShortCo/LongCo inp|cd inp|opt Dz` read $6.00 across the 9 APAC regions vs $5.50 in US/EU). That note listed only 5.1/5.2/5.4, codex-5.3, o4-mini and the embeddings. The catalog's GPT-5.5 DZ row (5.50/0.55/33.00) uses the US/EU majority and is correct.
- 2026-07-30 — Gotcha (grouping): do NOT partition Data Zone meters by effectiveStartDate. The 2026-06-01 tranche of the `5.5 *` and `5.3 codex *` DZ meters contains BOTH the 9 APAC regions at 1.20x AND `northeurope` at 1.10x, so a majority-within-date read returns the APAC price and silently corrupts a DZ row. Partition by APAC-region-set vs everything else, independent of date.
- 2026-07-30 — Watch/oddity: native `Azure Kimi` Data Zone OUTPUT meters are 1.25x Global for K2.6 and K2.7 Code only (`K2.6 Thinking Outp DZ` and `K2.7 Code Outp DZ` both $5.00 vs Global $4.00), while their input/cached are a clean 1.10x and K2/K2.5 outputs are a clean 1.10x. The Fireworks lane disagrees ($4.40 = 1.10x on both). Extends the 2026-07-23 K2.7 oddity to K2.6. No catalog impact — the tracked K2.6 DataZone row is the Fireworks lane.
- 2026-07-30 — Watch (still unfixed): the 2026-07-29 K2.6 Thinking DZ input anomaly persists — $3.00/M in northeurope, switzerlandnorth and switzerlandwest (eff. 2026-07-01) vs the 16-region $1.045/M majority (2.87x). The matching OUTPUT meter in those same 3 regions reads $5.00, identical to the majority, so only the input dimension is broken.
- 2026-07-30 — New billing dimension (outside the tracked lane): Model Studio publishes time-of-day discounts for qwen3.7-max/plus — 22:00–08:00 UTC+8 "night" tiers — in the Hong Kong, Frankfurt, US-Virginia and Tokyo deployments ONLY. The tracked Singapore/International lane carries the flat "Limited-time 50%/20% off" instead with no night tier, so tracked rates are unaffected. The schema does not model time-of-day pricing.
- 2026-07-30 — Anthropic dimensions the schema does not model, relevant to the `estimate`-confidence Sonnet 5 Foundry rows: `inference_geo: "us"` applies a 1.1x multiplier to all token categories on Claude 4.6+, and Anthropic's pricing page states the same 1.1x applies to Foundry US Data Zone Standard deployments — the documented mechanism by which Claude gets a Data Zone premium despite no Anthropic meter existing in the Azure retail API. Also newly noted: a "Fast mode" research-preview tier (Opus 5 / Opus 4.8 at $10 in / $50 out, first-party API only).
- 2026-07-30 — Re-verified unchanged this run (full 29,394-row Foundry sweep across 23 productNames, plus official direct pages): all 43 tracked Foundry rows match their live meters exactly; ZERO meters carry an effectiveStartDate >= 2026-07-29 (the maximum in the entire dataset is 2026-07-01), so the anticipated 2026-09-01 non-Global increase is still not staged — fourth consecutive run. Still absent from Foundry: Grok 4.4/4.5, any Qwen serverless per-token meter, any Claude/Anthropic meter, any GPT newer than 5.6, any Mistral cache meter, GLM 5.3, Kimi K2.8/K3, MiniMax M2.7. Direct pages unchanged for Anthropic (Sonnet 5 intro still ends 2026-08-31), OpenAI (tops out at 5.6 — and NO 5.6-pro variant exists; a third-party "Luna Pro/Terra Pro" claim is false), Gemini (3.6 Flash newest), xAI (4.5 newest, 4.4 still non-existent), DeepSeek (still no peak/off-peak on the international USD page; its changelog still stops at 2026-04-24), Moonshot (no K2.8; the Aug-31 V1 sunset affects only the legacy `moonshot-v1-*` series), Z.ai (no GLM 5.3), MiniMax ("Permanent 50% off" still live with no end date), Qwen (both promos live, qwen3.8-max still unpriced), Mistral, and Cohere (per-token Embed still delisted, so `derived` stays correct).
- 2026-08-01 — New Qwen watch item: Alibaba Model Studio officially schedules the tracked `qwen3.6-max-preview` for deprecation on 2026-10-10 and names `qwen3.7-max` as its replacement. Its International pricing remains $1.30/$7.80 per M for the ≤128K band (the catalog's row is unchanged apart from the warning). The fresh Azure Retail Prices API sweep returned 29,405 Foundry rows across 23 products; the 11 rows effective 2026-08-01 are provisioned managed-hour meters only (Azure OpenAI APAC Data Zone and Foundry Local), with no new serverless token listing or tracked-rate change. Qwen3.8 still has no per-token price, and the Qwen3.7 Max/Plus promotions remain live.
- 2026-08-02 — Watch update: DeepSeek's official Models & Pricing page now confirms that the API will soon adopt peak/off-peak pricing, with all billing items charged at 2x during 09:00–12:00 and 14:00–18:00 Beijing time (UTC+8). It still gives no effective date beyond a future official announcement, and current V4 Pro/Flash USD rates are unchanged; do not stage the surcharge until dated official notice. Today's full Foundry sweep still found no new non-managed token rows or tracked-family rate/cache/tier changes.
- 2026-08-04 — MAJOR/watch: OpenAI's changelog confirms a direct-API price cut effective 2026-07-30 — GPT-5.6 Terra −20% (now $2.00/$0.20/$12.00 direct; long context $4.00/$0.40/$18.00) and Luna −80% (now $0.20/$0.02/$1.20 direct; long context $0.40/$0.04/$1.80); Sol unchanged ($5/$0.50/$30). A same-day full Azure Retail Prices sweep confirms the Foundry Global meters did NOT move (Terra still $2.50/$0.25/$15.00, Luna still $1.00/$0.10/$6.00, both still effective 2026-07-01), breaking the Foundry=direct 1:1 parity that had held since 2026-07-21 — Foundry Terra now runs 1.25x direct, Luna 5x direct. Catalog rate numbers unchanged (Foundry rows still match live meters); only the four Global GPT-5.6 Terra/Luna rows' notes/sourceNotes were updated to flag the gap. Watch for Foundry meters to follow the cut.
- 2026-08-04 — Resolved: qwen3.8-max now has a published per-token price on Model Studio's International endpoint — $2.00/M input, $6.00/M output, single 0<Token≤1M band, Non-Thinking and Thinking modes identical, GA (not preview), no promo label (unlike qwen3.7-max's discount). Closes the watch item open since 2026-07-23. Cached input derived at 10% of input ($0.20/M) per the same context-cache rule as the rest of the family; 1M-token free quota for 90 days. A separate Global deployment scope exists at $1.65/$4.951 — tracked lane stays International for comparability with the rest of the Qwen rows. Qwen3.7 Max (50% off) and Plus (20% off) promos still live; qwen3.6-max-preview's 2026-10-10 deprecation (qwen3.7-max as replacement) unchanged.
- 2026-08-04 — Gotcha: a broad multi-provider sweep agent reported both a real change (the GPT-5.6 direct cut above — confirmed) and a false one (claimed Gemini 3.5 Flash had dropped to $0.80/$4.00) in the same run; the false claim was refuted by a raw DOM/table dump showing the actual rate is unchanged at $1.50/$0.15/$9.00, page last updated 2026-07-30. Single-agent price-change claims must be re-verified with a raw DOM/table dump before touching the catalog — a sweep being right about one provider is no evidence it's right about another in the same report.
- 2026-08-04 — Billing reconciliation (first real exports, strengthens confidence, no catalog change): a July Azure cost CSV shows a constant FX rate of 0.809050 CHF/USD, all 27 billed Foundry meters map to already-tracked families, and GPT-5.6 Sol/Terra bill separate cache-WRITE meters ('Cd Wr', 1.25x input — $40.80 of $174.56 July Sol spend), a billing dimension this schema doesn't model. A DeepSeek platform usage export (Aug 2–3 only, daily granularity) reconciles exactly to official direct rates (V4 Pro $0.435/$0.003625/$0.87, Flash $0.14/$0.0028/$0.28) with no surcharge visible — daily aggregates can't test the pending peak/off-peak change though. Also: DeepSeek's changelog now extends to 2026-07-31 (V4-Flash API beta), superseding the earlier "stops at 2026-04-24" note. Today's Azure sweep re-confirmed all known absences and that nothing ≥2026-08-02 is staged (max effectiveStartDate 2026-08-01, provisioned hourly meters only).
- 2026-08-05 — Re-verified unchanged this run (full 29,405-row Foundry sweep across 23 productNames + official direct pages): all tracked Foundry rows match their live meters exactly (0 mismatches across 94 input/cached/output checks), and every tracked direct rate matches its official page. ZERO meters carry an effectiveStartDate >= 2026-08-02 — the max in the entire dataset is 2026-08-01, and those 11 rows are provisioned/hourly meters only (Azure OpenAI APAC Data Zone Provisioned Managed Unit, Foundry Local), so the rumored 2026-09-01 non-Global increase is still not staged. Still absent from Foundry: Grok 4.4/4.5, any Qwen serverless per-token meter (the `Qwen models` product is 8 fine-tuning hosting SKUs for customer-deployed Qwen3-32B only), any Claude/Anthropic meter, any GPT newer than 5.6, any Mistral cache meter, GLM 5.3, Kimi K2.8/K3, MiniMax M2.7, DeepSeek V5. Direct pages unchanged for OpenAI, Anthropic (Sonnet 5 intro still ends 2026-08-31), Gemini (3.6 Flash $1.50/$0.15/$7.50 and 3.5 Flash $1.50/$0.15/$9.00, page stamped 2026-07-30 — the earlier $0.80/$4.00 claim re-refuted a second time), xAI, Mistral, DeepSeek, Qwen, Z.ai, Moonshot, MiniMax, Cohere (per-token Embed still delisted, so `derived` stays correct).
- 2026-08-05 — Watch (unresolved, second consecutive run): the GPT-5.6 Foundry/direct gap persists. Foundry Global meters are still on their original 2026-07-01 tranche — Terra $2.50/$0.25/$15.00, Luna $1.00/$0.10/$6.00 — while OpenAI's direct rates were cut on 2026-07-30 to $2.00/$0.20/$12.00 and $0.20/$0.02/$1.20. Foundry Terra now runs 1.25x direct and Luna 5x direct; Sol is unaffected ($5/$0.50/$30 on both). All 72 Sol/Terra/Luna meters carry a uniform effectiveStartDate of 2026-07-01.
- 2026-08-05 — New (extends the 2026-08-04 billing reconciliation): cache-write `Cd Wr` meters are confirmed on the published rate card for ALL THREE GPT-5.6 models on BOTH Global and DataZone, at exactly 1.25x the corresponding input meter — previously only Sol was known, and only from an invoice. Also: the PP (priority processing) tier exists for ShortCo only at exactly 2x Std; there are no LongCo PP meters. Cache-write remains a billing dimension this schema does not model.
- 2026-08-05 — GOTCHA (promo conflation, high value): a search for "qwen3.7-max 50% off end date" surfaces a page announcing the discount ends 2026-07-31 — that page is `help.aliyun.com/en/lingma/...`, i.e. Alibaba's Lingma/Qoder coding-IDE product, NOT the Model Studio per-token API. THREE distinct discount mechanisms share the "qwen3.7-max 50% off" name: the Model Studio API rate (no published end date, still live), the Qoder IDE promo (ended 2026-07-31), and Token Plan Team Edition credits (ended 2026-07-22). Do not let a hit on either of the latter two trigger a catalog rate change. Model Studio's billing page still shows the bare "$2.5 Limited-time 50% off" label with no end-date text.
- 2026-08-05 — Scope (untracked, deliberate) — serverless per-token meters found in this sweep that are NOT in the catalog and were not added: Grok "Code Fast 1" (Global $0.20/$1.50, DZ 1.1x, live since 2025-09-01), Mistral "Large 3" (Global $0.50/$1.50, DZ $0.55/$1.65, live since 2025-12-01 — note this sits far BELOW Mistral's own direct FAQ quote of $2/$6 for Mistral Large, so cross-check the identity before ever treating it as a gap), and DeepSeek "V3.2 SP" (an exact price duplicate of plain V3.2 on every dimension; the meaning of "SP" is unresolved from the API alone). Per the standing rule, run `git log -S` on a model name before treating any of these absences as an oversight.
- 2026-08-05 — Meter-naming gotchas found this run: (1) `FW DeepSeek-V4-Pro Ch Inp DZ Tokens` — "Ch Inp" is Fireworks' abbreviation for Cached Inp on this one line ($0.165/M, matches the tracked row); every other Fireworks family uses "Cache Inp". (2) `Grok4 Fast` (no space) is a frozen pre-rebrand meter name whose prices are identical to the current `Grok 4.1` meters ($0.20/$0.50 Global) — a naming-history duplicate, not a separate model.
- 2026-08-05 — MAI Models update: the product grew from 12 to 18 meters, but the mystery is unchanged — the original cryptic `MAI o` through `MAI z` text meters are byte-for-byte the same (same letters, same $1.75/M–$51.70/M prices, same 2026-06-01 date, armSkuName still just "MAI <letter>", still unmappable to any model identity). The 6 new meters are clearly-named image-generation SKUs (`Image 2` / `Image 2e`, priced per 1K tokens — Image 2 Global $5.00 in / $33.00 out, DZ 1.1x) and are out of schema scope. Keep the text-meter watch item open.
- 2026-08-05 — MiniMax: found an undocumented Priority tier at exactly 1.5x standard ($0.45/$1.80 for the ≤512K band, $0.90/$3.60 above it) — a tier dimension the schema does not model, deliberately not tracked. Separately, the direct page confirms why the two tracked Foundry DZ rows differ on cache: legacy M2.5/M2.1/M2 have a $0.03/M cached read while current M3/M2.7 have $0.06/M, so the catalog's $0.033 and $0.066 DZ figures are each a clean 1.1x. "Permanent 50% off" is still shown on M3 with no end date.
- 2026-08-05 — Research-tool notes: OpenAI's pricing page has moved — `platform.openai.com/docs/pricing` now 301-redirects to `developers.openai.com/api/docs/pricing` (use the latter); the OpenAI blog post and Help Center changelog both return HTTP 403 to WebFetch, so the pricing page is the only first-party quotable source. Mistral's marketing page `mistral.ai/pricing` will not render its API table via WebFetch (client-side rendered; returns only the consumer Vibe subscription plans) — use `docs.mistral.ai/models/model-cards/<model-id>` instead, which carries the per-token rate directly.
- 2026-08-05 — Informational, no pricing (do not add rows): OpenAI named its next major model "Astra" on 2026-08-01, announced via solved math/CS problems and explicitly NOT released — no model card, no API identifier, no price. A "Kimi K3.1 in August" story is third-party speculation with no official page, benchmarks, or date. Moonshot's August 31 sunset still applies only to the legacy `moonshot-v1-*` series and does not touch K2.6/K2.7/K3. Anthropic's own pricing page this run directly confirms the documented Foundry mechanism behind the `estimate`-confidence Sonnet 5 rows: the `inference_geo: "us"` 1.1x multiplier applies to Claude 4.6+ and the same 1.1x applies to US Data Zone Standard deployments on Foundry.
- 2026-08-06 — Full re-verification run, ZERO changes: all 67 catalog rows match their official sources. Foundry side (29,405-row sweep, 23 productNames, 1,694 distinct product/meter pairs): 40 tracked Foundry rows checked, 0 mismatches. Direct side: every tracked rate on OpenAI, Anthropic, Gemini, xAI, Mistral, Cohere, Qwen, DeepSeek, Z.ai, Moonshot and MiniMax matches. Still absent from Foundry: Grok 4.4/4.5, any Qwen serverless per-token meter, any Claude/Anthropic meter (so the Sonnet 5 Foundry rows stay `estimate`), any GPT newer than 5.6, any Mistral cache meter, GLM 5.3, Kimi K2.8/K3, MiniMax M2.7, DeepSeek V5. No new model anywhere gained a per-token price that the catalog tracks.
- 2026-08-06 — CHANGED (supersedes the 2026-08-02 note): DeepSeek's pricing page NO LONGER describes the peak/off-peak mechanism. The "2x during 09:00–12:00 and 14:00–18:00 Beijing time" wording is gone — verified twice by independent raw-DOM reads, with case-insensitive hit counts of 0 for "peak", "09:00", "14:00", "Beijing" and "UTC+8" across the full page text. Footnote (2) now reads only: "We plan to raise the overall pricing for DeepSeek API services in the near future, with a significant increase expected. Please plan your usage accordingly. The specific pricing plan will be subject to official notice." So the warning is now vaguer, not more concrete, and still carries no effective date. V4 Pro ($0.435/$0.003625/$0.87) and V4 Flash ($0.14/$0.0028/$0.28) are unchanged. Footnote (1) also says Responses API support for deepseek-v4-pro arrives "early August 2026".
- 2026-08-06 — Watch (3rd consecutive run, unmoved): the GPT-5.6 Foundry/direct gap persists. All 72 Sol/Terra/Luna meters still carry a single uniform effectiveStartDate of 2026-07-01 — one value, no second tranche — so Foundry Terra is still $2.50/$0.25/$15.00 and Luna $1.00/$0.10/$6.00 against direct rates cut on 2026-07-30 to $2.00/$0.20/$12.00 and $0.20/$0.02/$1.20. Foundry Terra = 1.25x direct, Luna = 5.0x direct, Sol at parity. Confirmed by full dump plus two live server-side exact-meterName queries. Also re-confirmed: GPT-5.6's DataZone lane is 13 US/EU regions with ZERO APAC overlap, so this family is a uniform 1.10x and the APAC 1.20x premium does not touch it.
- 2026-08-06 — Watch (6th consecutive run): the rumored 2026-09-01 non-Global increase is still NOT staged. Zero meters carry an effectiveStartDate >= 2026-08-02; the max in the whole dataset is 2026-08-01, and those 11 rows are provisioned/hourly only (Azure OpenAI `Provisioned Managed Data Zone Unit` $1.20/hr across the 9 APAC DZ regions, and Foundry Local `Provisioned Managed Unit` $17.167/hr). Note the APAC provisioned unit is $1.20/hr vs $1.10/hr in US/EU — the same 1.20x-vs-1.10x APAC premium, now visible in the provisioned lane too.
- 2026-08-06 — Method gotcha (high value): the Azure Retail Prices API REJECTS all server-side `effectiveStartDate` filters — both `effectiveStartDate gt datetime'2026-08-01'` and `effectiveStartDate ge 2026-08-02` return `{"Error":{"Code":"BadRequest","Message":"Invalid OData parameters supplied"}}`. "Is anything newer staged?" can only be answered from a full paged dump, never from a filtered query.
- 2026-08-06 — Extends the 2026-07-28 northeurope note: the rounded-price outlier is not Fireworks-DeepSeek-specific. A systematic scan of every meter carrying a northeurope row found exactly 7 divergences from their region majority, including `FW GLM 5.1 Cache Inp DZ Tokens` at $0.29 vs the $0.286 majority (new this run), the three known FW DeepSeek V4 Pro meters, and three legacy OpenAI regional meters. Region grouping stays mandatory for reasons unrelated to APAC.
- 2026-08-06 — MAI Models progress (watch still open): the 12 cryptic `MAI o`–`MAI z` text meters resolve into just 6 priced lanes, not 12 — six Global/DataZone pairs at exactly 1.10x, separable by region count (32-region APAC-inclusive = Global, 19-region = DataZone): s/y $1.75→$1.925, t/z $1.75→$1.925, p/v $5.00→$5.50, q/w $8.00→$8.80, u/o $19.50→$21.45, r/x $47.00→$51.70. Model identity is still unmapped and `skuName` is still just `MAI <letter>`. Tempting but UNVERIFIED: $1.75/$1.925 is byte-identical to GPT-5.2 / GPT-5.3-chat input pricing, but `MAI q` $8.00 and `MAI r` $47.00 map to nothing — do not act on the correspondence.
- 2026-08-06 — Watch: `Grok 4.1 Fast` has disappeared from xAI's OWN direct pricing page (docs.x.ai now lists only grok-4.5, grok-build-0.1, grok-4.3 and three grok-4.20 snapshots). The catalog's Grok 4.1 Fast row is Foundry-sourced and its meter is still live at $0.20/$0.50 Global, so the row is not wrong — but if xAI has retired the model, the Foundry listing may be on borrowed time. Recheck the Foundry side before assuming the row stays valid.
- 2026-08-06 — Scope (untracked, deliberate — new this run): xAI `grok-build-0.1` has an official direct price ($1.00/$0.20/$2.00 under 200K, $2.00/$0.40/$4.00 above, 256K ctx) and three `grok-4.20-*-0309` snapshot variants price identically to Grok 4.3 — excluded on the same precedent as Grok "Code Fast 1". Google's `Gemini 3.1 Pro Preview` ($2.00/$0.20/$12.00 ≤200K, $4.00/$0.40/$18.00 above) is excluded because the Gemini lane deliberately tracks Flash only, same precedent as Flash-Lite. MiniMax `H3` on MiniMax's pricing page is a video/audio generation family, not an LLM chat model. Kimi K2.7 Code has a "highspeed" variant at exactly 2x ($1.90/$0.38/$8.00) — a tier dimension the schema does not model.
- 2026-08-06 — RESOLVED: the 2026-08-05 caution about Mistral `Large 3` (Foundry Global $0.50/$1.50 sitting far below a "$2/$6" figure quoted in Mistral's FAQ) is closed. The official model card at docs.mistral.ai/models/model-cards/mistral-large-3-25-12 publishes $0.50/$1.50 per M, released 2025-12-02 — an exact match to the Foundry Global meter, so the "$2/$6" figure does not correspond to the current model. Large 3 stays untracked by design (older and larger than Medium 3.5, not a newer flagship). Tool note: the model-card slug bakes in the version — `.../mistral-medium-3-5-26-04` works, `.../mistral-medium-3.5` 404s.
- 2026-08-06 — Provenance confirmations worth keeping: (a) Alibaba Model Studio's billing text states outright that "explicit cache creation is billed at 125% of the standard input price, and cache hits at 10%" — this is the official basis for every derived Qwen cached-input figure in the catalog. (b) Anthropic's pricing page again states verbatim that `inference_geo: "us"` applies a 1.1x multiplier on Claude 4.6+, and that Azure US Data Zone Standard deployments are equivalent and carry the same 1.1x — the documented basis for the `estimate`-confidence Sonnet 5 Foundry rows. (c) Alibaba's dedicated decommissioning page (help.aliyun.com/en/model-studio/model-depreciation), not just the pricing page, confirms qwen3.6-max-preview retires 2026-10-10 with qwen3.7-max as replacement.
- 2026-08-06 — Informational, no catalog impact: xAI's docs site now titles itself "SpaceXAI Docs" following SpaceX's acquisition of xAI, but the Grok product names, the API, and the docs.x.ai domain are all unchanged. Anthropic's pricing page refers to the same product as both "Claude Mythos Preview" and "Claude Mythos 5 (limited availability)", both linking to anthropic.com/glasswing — one model, two labels, not a second model. Moonshot's Aug 31 sunset text is attached only to the Moonshot V1 card and still does not touch K2.6/K2.7/K3.
- 2026-08-07 — Full re-verification sweep, ZERO rate changes. Foundry: fresh 29,405-row dump across 23 productNames / 1,694 distinct product|meter pairs; 38 tracked Foundry rows checked, 0 mismatches. Direct: every tracked rate on OpenAI, Anthropic, Gemini, xAI, Mistral, Cohere, Qwen, DeepSeek, Z.ai, Moonshot and MiniMax matches its official page. Still absent from Foundry: Grok 4.4/4.5, any Qwen serverless per-token meter (the `Qwen models` product is 8 fine-tuning SKUs for Qwen3-32B only), any Claude/Anthropic meter (so Sonnet 5 Foundry rows stay `estimate`), any GPT newer than 5.6, any Mistral cache meter (43 meterNames, 0 match `cach|cd |ch inp`), GLM 5.3, Kimi K2.8/K3, MiniMax M2.7, DeepSeek V5, any OpenAI "Astra" meter.
- 2026-08-07 — CORRECTED (provenance, no rate change): the three OpenAI embedding rows are `tier: "Global"` Foundry rows but carried `sourceNote: "OpenAI pricing page."`. That page does not publish per-token embeddings rates and, per Internet Archive snapshots from 2026-06-05 through 2026-08-04, has not in that window. Rates are right ($0.13/$0.02/$0.10 per M) and confidence stays `official` — the Azure meter is the official publication, same treatment as the Cohere Embed v4 Foundry rows. Corroborated independently by OpenAI's embeddings guide, which publishes pages-per-USD (62,500 / 9,615 / 12,500 at ~800 tokens/page) inverting to exactly those rates. Do NOT downgrade these to `derived` — `derived` means back-calculated from a billing export, which is not the case here.
- 2026-08-07 — GOTCHA (high value, WebFetch fabrication): WebFetch on developers.openai.com/api/docs/pricing returned a clean embeddings table asserting $0.13/$0.02/$0.10 — figures that do not appear anywhere on that page. Same class as the Gemini "$0.80/$4.00" fabrication but far harder to catch, because the fabricated numbers were CORRECT. A WebFetch confirmation is never evidence that a figure is still published; confirm publication with a raw DOM dump.
- 2026-08-07 — GOTCHA (high value, Alibaba page restructure): the Model Studio pricing page moved to `www.alibabacloud.com/help/en/model-studio/model-pricing` and is now built from 62 region tab widgets (`div.tab-item`), whose DEFAULT ACTIVE TAB is China (Beijing) rendering CNY Beijing-endpoint prices — a naive dump now silently returns the wrong lane and wrong currency. To extract the tracked lane: querySelectorAll('div.tab-item'), filter to "Singapore", click them, then filter tables by visibility (offsetParent || getClientRects().length) before walking the rowSpan grid; the Deployment scope column reads International under that tab. Two related traps: `help.aliyun.com/en/...model-pricing` serves CNY even on the /en/ path (only the alibabacloud.com host serves USD; CNY = USD x 7.494 on every tracked row, a useful cross-check), and `help.aliyun.com/en/model-studio/models` is no longer the pricing page at all (a ~2.2KB landing page with 0 tables) so it will produce a false negative for any model.
- 2026-08-07 — Watch (4th consecutive run, unmoved): the GPT-5.6 Foundry/direct gap persists. All 72 Sol/Terra/Luna meters still carry ONE uniform effectiveStartDate of 2026-07-01 with no second tranche — Foundry Terra $2.50/$0.25/$15.00 vs direct $2.00/$0.20/$12.00 (1.25x) and Luna $1.00/$0.10/$6.00 vs direct $0.20/$0.02/$1.20 (5.0x); Sol at parity. OpenAI's direct page now also publishes cache-write inline at 1.25x input, matching the Foundry `Cd Wr` meters.
- 2026-08-07 — Watch (8th consecutive run): the rumored 2026-09-01 non-Global increase is still NOT staged. Max effectiveStartDate in the whole 29,405-row dataset is 2026-08-01, and those 11 rows are provisioned/hourly only (Azure OpenAI `Provisioned Managed Data Zone Unit` $1.20/hr across the 9 APAC DZ regions, Foundry Local `Provisioned Managed Unit` $17.167/hr). Reminder: server-side effectiveStartDate filters return BadRequest, so this can only be answered from a full paged dump.
- 2026-08-07 — UPCOMING, act on/after 2026-09-01: Claude Sonnet 5's introductory pricing ends 2026-08-31. Anthropic's page now renders the successor rate as its own row — "Claude Sonnet 5 starting September 1, 2026 | $3 | $3.75 | $6 | $0.30 | $15" — so the catalog's intro rows ($2/$0.20/$10, incl. the two `estimate` Foundry rows) will need to be retired in favour of the standard rows at the start of September. Both 1.1x statements re-confirmed verbatim this run (`inference_geo: "us"` on Claude 4.6+, and the Azure US Data Zone Standard equivalence).
- 2026-08-07 — Watch escalated: `Grok 4.1 Fast` is absent from xAI's own pricing docs for a SECOND consecutive run (the string does not occur anywhere on docs.x.ai/developers/pricing; lineup is grok-4.5, grok-build-0.1, grok-4.3 and three grok-4.20-*-0309 snapshots). The Foundry side shows no sign of retirement though — `Grok 4.1 Inp/Outp Glbl Tokens` still spans a full 41 regions at $0.20/$0.50 per M, eff. 2026-01-01, plus a live DZ lane at $0.22/$0.55. The catalog row is Foundry-sourced so it is not wrong, but treat Foundry delisting as expected rather than hypothetical.
- 2026-08-07 — Anomalies re-checked, all unchanged: (a) the native `K2.6 Thinking Inp DZ` outlier persists — $3.00/M in exactly northeurope, switzerlandnorth and switzerlandwest (eff. 2026-07-01) vs the 16-region $1.045/M majority (2.87x), while the matching output meter reads $5.00 uniformly, so only the input dimension is broken; no catalog impact since the tracked K2.6 DataZone row is the Fireworks lane. (b) MAI Models still 18 meters / 510 rows, the 12 cryptic text meters byte-identical to prior runs and still resolving to 6 Global/DZ pairs at 1.10x, model identity still unmapped. (c) northeurope rounded-price outliers unchanged (FW GLM 5.1 Cache Inp DZ $0.29 vs $0.286; FW DeepSeek V4 Pro $1.93/$0.17/$3.83 vs $1.925/$0.165/$3.828) — region grouping stays mandatory.
- 2026-08-07 — DeepSeek: the peak/off-peak surcharge wording is still gone (2nd consecutive run) — case-insensitive hit counts of 0 for "peak", "off-peak", "09:00", "14:00", "Beijing" and "UTC+8" across the full page text. Footnote (2) still reads only that a significant across-the-board increase is planned "in the near future... subject to official notice", with no effective date, so nothing is stageable. Newest changelog entry is 2026-07-31 (DeepSeek-V4-Flash public beta, `DeepSeek-V4-Flash-0731`); no V5. Footnote (1)'s promise of Responses API support for `deepseek-v4-pro` in "early August 2026" is now overdue — the feature matrix still shows it unsupported.
- 2026-08-07 — Scope (untracked, deliberate — new this run): Z.ai `GLM-5V-Turbo` ($1.20/$0.24/$4.00) is the multimodal sibling of GLM-5-Turbo, outside the 5.x text lane; xAI `grok-voice-think-fast-2.0` is a speech model at $0.08/min; MiniMax `MiniMax-H3-Context-IR` ($0.90/$3.60) belongs to the H3 video/audio family, not an LLM chat lane; Mistral `Small 4` (v26.03, $0.15/$0.60) is smaller and older than the tracked Medium 3.5. Also informational: Qwen's GLOBAL (untracked) lane now labels qwen3.7-max "night 80% off, daytime 50% off", writing the time-of-day dimension into the discount label — the tracked International lane still carries a flat 50% off with no night tier.
- 2026-08-07 — Tooling caution: when several agents run browser work concurrently they share one browser pane, and a tab navigated by one agent can be repointed by another mid-run (observed: a dump whose location.href resolved to a different provider's docs than the URL just navigated to). Always echo `location.href` and `document.title` inside the JS payload and verify the origin before trusting a dump; dedicate a tab per agent.
- 2026-08-08 — Scheduled check, ZERO catalog changes. Fresh full Foundry sweep (29,405 rows / 23 productNames / 1,694 product|meter pairs — identical counts to 8/6–8/7): max effectiveStartDate still 2026-08-01 and those rows are provisioned/hourly only, so the rumored 2026-09-01 non-Global increase is still NOT staged (9th consecutive run); all 72 GPT-5.6 meters still carry the single uniform 2026-07-01 tranche (5th consecutive run — Foundry Terra 1.25x / Luna 5.0x direct gap persists); 10-meter spot-check across GLM/MiniMax/DeepSeek (native + FW)/Grok 4.3/Kimi K2.6/Mistral MM3.5/Cohere Embed/OpenAI embeddings all exact matches. Still absent from Foundry: Grok 4.4/4.5, any Claude/Anthropic meter, OpenAI Astra, any GPT newer than 5.6, any Qwen serverless per-token meter, GLM 5.3, Kimi K2.8/K3, MiniMax M2.7, DeepSeek V5, any Mistral cache meter. Note scope: direct provider pages were NOT re-read via raw DOM this run (yesterday's 8/7 sweep covered them); news side was WebSearch-only.
- 2026-08-08 — Gotcha (source quality): a Microsoft Q&A answer (learn.microsoft.com/en-us/answers/questions/5962521) claims Azure synced the GPT-5.6 Terra/Luna direct-API cuts "effective 2026-08-01". The Azure Retail Prices API refutes this for Foundry serverless meters — all 72 GPT-5.6 meters remain on the 2026-07-01 tranche at the old rates. Treat Microsoft Q&A answers as third-party claims, not official rate publications; only the retail API or the pricing page count.
- 2026-08-08 — MAI lead (unverified, watch still open): Microsoft's Build 2026 announcement (microsoft.ai/news, 2026-06-02) named MAI models available on Foundry — MAI-Thinking-1, MAI-Code-1-Flash, MAI-Image-2.5(-Flash), MAI-Transcribe-1.5, MAI-Voice-2(-Flash). The cryptic `MAI o`–`MAI z` text meters are effective 2026-06-01, matching the Build timing, so Thinking-1 and Code-1-Flash are candidate identities for some of the 6 priced lanes — but no public per-token rate card exists to map letters to models, so the correspondence stays unverified. Do not add rows on this basis.
- 2026-08-09 — MAJOR/RESOLVED (watch open since 2026-07-20): the "rumored 2026-09-01 non-Global price increase" is now officially confirmed and documented. Microsoft Foundry Blog post "Microsoft Foundry model deployment pricing update" (techcommunity.microsoft.com/blog/azure-ai-foundry-blog/microsoft-foundry-model-deployment-pricing-update/4535385, published 2026-07-09, updated 2026-07-27 "VERSION 2.0", Chris Hoder) — effective 2026-09-01: Global unchanged; US Data Zone stays 1.10x; EU Data Zone rises to 1.20x; APAC Data Zone becomes official at 1.20x (matching the premium already visible in meters since 2026-06-01); Regional outside the US rises to 1.25x–1.50x by country (AU/IN/ID/MY/NZ 1.25x; AT/BE/CA/DK/DE/IT/MX/PL/ZA/ES/SE/CH/AE 1.30x; JP/KR/TW 1.35x; FR/HK/IL/NO/QA/UK 1.40x; BR/EU-North/EU-West/SG 1.50x). CRITICAL grandfather clause, verbatim: "the planned premiums apply only to models launched on or after September 1, 2026. Customers who stay on their current models see no price increase" — so NO currently tracked catalog row changes on Sept 1; the premiums attach only to models Foundry launches on/after that date (watch new-model DZ/regional meters for the 1.20x EU shape). Sole non-grandfathered exception is PTU ("the increase applies to all customers with EU Data Zone or Regional PTUs outside of the US"), out of schema scope. Verified via two independent raw-DOM reads (byte-identical text). Implication for future sweeps: the retail API showing "nothing staged" (max effectiveStartDate 2026-08-01, 10th consecutive run) is consistent with this design and is no longer a useful proxy for the item — the watch is CLOSED; what remains is checking the DZ/regional multipliers on any model that lands on Foundry after 2026-09-01.
- 2026-08-09 — Scheduled check, ZERO catalog rate changes. Fresh full Foundry sweep: 29,405 rows / 23 productNames / 1,694 product|meter pairs — identical to 8/8; all 72 GPT-5.6 meters still on the single 2026-07-01 tranche (6th consecutive run; Terra 1.25x / Luna 5.0x direct gap persists — a second Microsoft Q&A thread now asks why, still no official sync). Spot-checks exact on Grok 4.3 (incl. 2x long-context), FW GLM 5 DZ, FW MiniMax 3 DZ, DeepSeek V4 Pro native+FW DZ (usgov and northeurope outliers unchanged), Kimi K2.6 FW DZ (22/22 uniform), Mistral MM3.5, Cohere Embed v4 Glbl/DZ, OpenAI embeddings. All known absences re-confirmed (no Claude/Anthropic meter, no Grok 4.4/4.5, no Qwen serverless per-token meter, nothing newer than GPT-5.6, no Astra, no Mistral cache meter, no GLM 5.3, no Kimi K2.8/K3, no MiniMax M2.7 meter, no DeepSeek V5). MAI Models still 18 meters, the 12 cryptic text meters byte-identical. Sweep detail worth keeping: the three OpenAI embedding Global meters live under productName `Azure OpenAI` (not `Azure OpenAI Embedding`, which holds only the DZ large/small meters).
- 2026-08-09 — Gotcha/RESOLVED (MiniMax URL restructure, NOT a price change): `platform.minimax.io/document/price` is a hard 404 and `platform.minimax.io/docs/price` an app-level soft-404; the pricing page moved to `platform.minimax.io/docs/guides/pricing-paygo` (found via the docs sitemap `platform.minimax.io/docs/llms.txt` — useful discovery trick). Raw-DOM read there confirms M3 at struck-through $0.60/$0.12/$2.40 → $0.30/$0.06/$1.20 (≤512K band; >512K exactly 2x) with the verbatim badge "Permanent 50% off" and ZERO expiration language anywhere on the page — a third-party claim that the discount was a lapsed 7-day launch promo is FALSE. M2.7 flat $0.30/$0.06/$1.20 (highspeed 2x, cache write $0.375 — a dimension the schema doesn't model); M2.5/M2.1/M2 confirmed under a collapsed "Legacy Models" accordion at cached $0.03 (corroborates the 2026-08-05 $0.03-vs-$0.06 split).
- 2026-08-09 — Grok 4.1 Fast watch clarified (not new news): xAI retired grok-4-1-fast-reasoning/non-reasoning from its direct API on 2026-05-15 (docs.x.ai/developers/migration/may-15-retirement; requests auto-redirect to and bill as grok-4.3) — that is why the model vanished from xAI's pricing docs. A "deprecated May 15 / retires Aug 15" framing seen in aggregators is Oracle OCI's hosting grace period, not xAI or Azure. Foundry meters remain fully live and unchanged ($0.20/$0.50 Global, $0.22/$0.55 DZ, plus the frozen "Grok4 Fast" naming duplicate), so the Foundry-sourced catalog row stands.
- 2026-08-09 — Watch (MAI, still open): third parties (models.dev, Neowin, datanorth.ai) now publish a NAMED price for MAI-Code-1-Flash — $0.75 in / $0.075 cached / $4.50 out per M, via GitHub Copilot — which matches NO cryptic letter-meter pair (nearest neighbors $1.75 and $5.00), so it looks like Copilot-specific billing rather than a Foundry serverless rate; do not map letters to models on this basis. Also informational: DeepSeek's vague price-increase footnote got broad same-day press pickup on 2026-08-06 (Bloomberg, SCMP, TechNode) — still no magnitude and no effective date, nothing stageable.
- 2026-08-10 — RESOLVED (watch open since 2026-07-20): the Qwen3.7-Max 50%-off promo now has an OFFICIAL end date — 2026-08-31 — published on Alibaba Cloud's campaign page (alibabacloud.com/en/campaign/qwen-discount, verbatim in two places: "runs until August 31, 2026"; discount covers all 4 billing items incl. Explicit Cache Creation/Hit; Buy Now buttons resolve to the Model Studio console with serviceSite=international, so it IS the tracked per-token lane, not the Lingma/Qoder or Token Plan promos). The Model Studio pricing page itself STILL shows only the undated "Limited-time 50% off" badge (raw-DOM regex scans for "august 31"/"until"/"expir" = 0 hits). Qwen3.7-Plus 20% off has NO end date anywhere (the campaign page never mentions Plus). ACT on/after 2026-09-01: revert the Qwen3.7 Max row to list $2.50/$0.25/$7.50 — same day as the Sonnet 5 intro-pricing end.
- 2026-08-10 — Scheduled check, ZERO rate changes. Fresh full Foundry sweep: 29,405 rows / 23 productNames / 1,694 product|meter pairs — identical to 8/9; max effectiveStartDate still 2026-08-01 (provisioned/hourly only); all 72 GPT-5.6 meters still on the single 2026-07-01 tranche (7th consecutive run — Terra 1.25x / Luna 5.0x direct gap persists); spot-checks exact on FW GLM 5 DZ, FW MiniMax 3 DZ, DeepSeek V4 Pro native+FW DZ (northeurope outlier unchanged), Kimi K2.6 FW DZ, Mistral MM3.5 (still no cache meter), Cohere Embed v4, OpenAI embeddings, Grok 4.3 (incl. 2x L variant), Grok 4.1 (Fast) Global/DZ. All known absences re-confirmed (no Claude/Anthropic meter, no Grok 4.4/4.5, no Qwen serverless, nothing newer than GPT-5.6, no Astra, no Mistral cache meter, no GLM 5.3, no Kimi K2.8/K3 meter, no MiniMax M2.7 meter, no DeepSeek V5). MAI Models unchanged (18 meters, 12 cryptic letter meters byte-identical); K2.6 Thinking DZ input anomaly unchanged.
- 2026-08-10 — Gotcha (aggregator contamination): LiteLLM's cost map wrongly applied OpenAI's 2026-07-30 direct cuts to twelve Azure GPT-5.6 rows on 2026-08-05 (github.com/BerriAI/litellm/issues/36192, maintainer confirmed "Azure has not updated their pricing yet"). Any downstream aggregator claiming Foundry synced the Terra/Luna cut is likely echoing this bug — only the retail API counts.
- 2026-08-10 — Observation (long-standing, not a change): the frozen "Grok4 Fast" naming-duplicate meter's DZone lane is NOT uniform — 4 regions (uksouth, switzerlandnorth, switzerlandwest, polandcentral) still carry an older 2025-09-01 tranche at $0.48/$1.90 vs the 18-region $0.22/$0.55 majority (eff. 2025-11-01). The primary "Grok 4.1" meters (which source the catalog row) are uniform — no catalog impact.
- 2026-08-10 — Context (explains a permanent absence): Claude on Foundry bills via Azure Marketplace "Claude Consumption Units" (CCUs) metered hourly (claude.com/blog/claude-in-microsoft-foundry), so per-token Anthropic meters will likely NEVER appear in the Azure Retail Prices API — the Sonnet 5 Foundry rows' `estimate` confidence is permanent-by-design until Anthropic/Microsoft publish per-token Foundry rates elsewhere. Grok 4.6: Musk says ~week of Aug 10 (after a missed Aug 7 target), 1.5T params — no model card, no API ID, no price yet; do not add until an official price page exists (remember the 4.4-skip precedent).
- 2026-08-11 — MAJOR/RESOLVED (supersedes the 2026-08-07 "UPCOMING, act on/after 2026-09-01" note): Anthropic's official @claudeai X account announced 2026-08-10 9:03pm that Claude Sonnet 5's introductory pricing ($2/$0.20/$10 per M) is now PERMANENT — the previously-documented Sept 1 2026 increase to $3/$0.30/$15 will NOT happen (verbatim: "that price will remain unchanged"). Verified via raw browser read of the live, verified post. Catalog's "Standard" rows (Direct + Foundry estimate) removed; "Intro" rows renamed and kept as the ongoing rate. WATCH: Anthropic's pricing docs page (platform.claude.com/docs/en/about-claude/pricing) still showed the old two-tier "$2/$10 through Aug 31" / "$3/$15 starting Sept 1" structure as of 2026-08-11 morning — pure lag, not a contradiction, but recheck in a future run that the docs page catches up to a single permanent rate, and that no other Sonnet 5 dimension (cache write/hit, batch) changed silently in the process.
- 2026-08-11 — Scheduled check (same day as the Sonnet 5 permanent-pricing PR #39), ZERO catalog changes. Fresh full Foundry sweep: 29,405 rows / 23 productNames / 1,694 product|meter pairs — byte-identical to prior runs; max effectiveStartDate still 2026-08-01 (provisioned/hourly only); all 72 GPT-5.6 meters still on the single uniform 2026-07-01 tranche (8th consecutive run — Foundry Terra 1.25x / Luna 5.0x direct gap persists, no sync). Spot-checks exact: FW GLM 5 DZ, FW MiniMax 3 DZ, DeepSeek V4 Pro native + FW DZ, Kimi K2.6 FW DZ, Mistral MM3.5 (still no cache meter), Cohere Embed v4, Grok 4.3 (incl. 2x L variant), Grok 4.1 Global/DZ. All known absences re-confirmed (no Claude/Anthropic meter, no Grok 4.4/4.5/4.6, no Qwen serverless per-token meter, nothing newer than GPT-5.6, no Astra, no GLM 5.3, no Kimi K2.8/K3, no MiniMax M2.7, no DeepSeek V5). Known anomalies unchanged: K2.6 Thinking DZ input $3.00 3-region outlier; Grok4 Fast DZ 4-region stale 2025-09-01 tranche; MAI Models still 18 meters (minor non-price wobble: `MAI w`'s usgovarizona row moved into the majority-price bucket, all prices identical). Direct pages re-verified unchanged by raw DOM: OpenAI (post-cut Sol/Terra/Luna rates match), xAI, DeepSeek (rates + footnote wording identical, "peak"/"Beijing"/"UTC+8" all 0 hits, changelog still stops 2026-07-31), Alibaba Model Studio (Qwen3.7 Max 50%-off and Plus 20%-off promos still live, no end-date text on the pricing page itself, qwen3.8-max unchanged, no new qwen model among 153 model IDs swept), Anthropic.
- 2026-08-11 — Watch (Sonnet 5 docs-page lag, day 2): platform.claude.com/docs/en/about-claude/pricing STILL shows the old two-tier structure — a "through August 31, 2026" row at $2/$2.50/$4/$0.20/$10 AND a "starting September 1, 2026" row at $3/$3.75/$6/$0.30/$15 (batch rows likewise $1/$5 vs $1.50/$7.50), with 0 page hits for "permanent"/"remain unchanged". The verified @claudeai X post (2026-08-10) remains the sole official source for the permanent $2/$0.20/$10 rate. Keep checking until the docs page catches up; if instead the docs page holds and the increase actually bills in September, the catalog call must be revisited.
- 2026-08-11 — GOTCHA (sweep-agent fabrication, same class as the 2026-07-28 false "Grok 4.4" report): a news-search agent claimed Grok 4.6 (1.5T params) was "CONFIRMED RELEASE, August 7, 2026 (xAI official)" citing docs.x.ai/developers/release-notes, with Grok 4.7 "a few weeks later" — REFUTED by direct raw-DOM reads of that exact page plus docs.x.ai/developers/models and /developers/pricing: 0 hits for "4.6", "4.7", "trillion" on all three; the release-notes changelog simply stops at 2026-07-31 (no August section); the models page is stamped "Last updated: July 9, 2026" and still flags grok-4.5 as the newest flagship. Musk's ~week-of-Aug-10 target (see 2026-08-10 note) is evidently not yet met. Demand a live docs.x.ai hit with a per-token price before adding any 4.6 row.
- 2026-08-11 — Scope (untracked, deliberate): OpenAI's direct pricing page now lists `gpt-5.6-cyber` under a "Cyber models" heading ("Our latest Daybreak models") at $12.50 in / $1.25 cached / $75.00 out per M (cache write $15.625, short-context only). Excluded from the catalog: specialized security variant outside the tracked Sol/Terra/Luna lane, and no Foundry meter exists (the 72-meter GPT-5.6 set is sol/terra/luna only) — same precedent as Gemini Flash Cyber and Flash-Lite.
- 2026-08-11 — Method note: the Alibaba Model Studio pricing page now renders 233 `div.tab-item` region widgets (was 62 on 2026-08-07). The 2026-08-07 extraction recipe still works unchanged: filter tabs whose text is exactly "Singapore" (63 this run), click them all, then read only visible tables (offsetParent/getClientRects) walking rowSpan continuation rows, and confirm the Deployment scope column reads "International".
- 2026-08-12 — MAJOR/RESOLVED (closes the Grok 4.6 watch): Grok 4.6 released 2026-08-12, verified by raw-DOM reads of docs.x.ai pricing/models/release-notes (models page stamped "Last updated: August 12, 2026"; sidebar now "Grok 4.6 Latest"). $2.00/$0.50/$6.00 ≤200K prompt, $4.00/$1.00/$12.00 above, 500K ctx, text+image in / text out, knowledge cutoff 2026-02-01, reasoning effort low/medium/high(default)/xhigh. Cached input HIGHER than 4.5 ($0.50 vs $0.30); in/out identical to 4.5. No parameter count published ("trillion" = 0 hits — the third-party "1.5T" figure remains unofficial); no Grok 4.7 anywhere. No Foundry meter (0 hits in today's full sweep). Direct row added via this PR.
- 2026-08-12 — RESOLVED (closes the Sonnet 5 docs-page-lag watch, day 3): platform.claude.com pricing docs caught up — single Sonnet 5 row $2/$2.50/$4/$0.20/$10, batch $1/$5, verbatim "is now the standard price ... will not occur" (note: the page never uses the word "permanent", so a text search for it returns 0 — search "standard price" instead). Both catalog sourceNotes updated. Also observed: the docs page now lists Claude Fable 5 and Claude Mythos 5 (limited availability) rows, both $10/$12.50/$20/$1/$50 — matches the catalog's existing rows.
- 2026-08-12 — Scheduled check, otherwise ZERO changes: full Foundry sweep byte-identical (29,405 rows / 23 productNames / 1,694 pairs; max effectiveStartDate still 2026-08-01, provisioned/hourly only); all 72 GPT-5.6 meters still on the single 2026-07-01 tranche (9th consecutive run — Terra 1.25x / Luna 5.0x direct gap persists); spot-checks exact (FW GLM 5 DZ, FW MiniMax 3 DZ, DeepSeek V4 Pro native+FW DZ, Kimi K2.6 FW DZ, Mistral MM3.5, Cohere Embed v4, Grok 4.3 incl. 2x L variant, Grok 4.1); all known absences re-confirmed (no Claude/Anthropic meter, no Grok 4.4/4.5/4.6 meter, no Qwen serverless, nothing newer than GPT-5.6, no Astra, no Mistral cache meter, no GLM 5.3, no Kimi K2.8/K3, no MiniMax M2.7, no DeepSeek V5). Anomalies unchanged: K2.6 Thinking Inp DZ 3-region $3.00 outlier (new minor detail: 2 usgov regions at a third price, $1.306/M — usgov-premium pattern, not new); Grok4 Fast DZ 4-region stale tranche; MAI Models 18 meters, letter prices byte-identical, region coverage grew 32→34 Global / 19→21 DZ with no price change. OpenAI direct page: post-cut Sol/Terra/Luna rates exact, "Astra" 0 hits, gpt-5.6-cyber unchanged; also notes "Priority processing was renamed Fast mode on July 30, 2026".
- 2026-08-12 — Watch/new: DeepSeek's pricing table now shows model version `DeepSeek-V4-Pro-0813` (no changelog entry for 0813 exists yet on api-docs.deepseek.com/updates, which still stops at 2026-07-31) and the Responses API + Anthropic API columns for V4 Pro flipped to supported — closes the overdue "early August" footnote item. Rates unchanged ($0.435/$0.003625/$0.87, Flash $0.14/$0.0028/$0.28); the price-increase footnote is unchanged in wording but renumbered from (2) to (1); "peak"/"Beijing"/"UTC+8" still 0 hits.
- 2026-08-14 — MAJOR/RESOLVED: DeepSeek's undated price-increase footnote is now DATED — peak/off-peak from 2026-08-16 16:00 UTC, off-peak exactly half peak, peak hours 01:00-04:00 and 06:00-10:00 UTC. Full new table. RESOLVED 2026-08-15 (see below): the "ACT ON 2026-08-16" scope call this bullet originally flagged is done — both Direct rows now carry `RateVariant`s (`from: "2026-08-16T16:00:00Z"`, same peak windows) and `src/lib/rates.ts` resolves the switch automatically; no manual edit was or is needed on 2026-08-16. Note the press (Bloomberg/SCMP/Fortune 2026-08-13) reported "4.5-4.7x" which is peak-output vs TODAY's output — the actual peak:off-peak ratio is exactly 2.0x.
- 2026-08-14 — GOTCHA (retire an old heuristic): the standing "0 hits for Beijing/09:00/14:00/UTC+8 means no surcharge" DeepSeek check is now a FALSE-NEGATIVE TRAP — the English page re-expressed the same windows in UTC (01:00-04:00, 06:00-10:00) while the Chinese locale page /zh-cn/quick_start/pricing/ still states 9:00-12:00 and 14:00-18:00 Beijing time, effective 北京时间 2026-08-17 00:00 = 2026-08-16 16:00 UTC. Same schedule, different timezone expression. Also: the CNY table is a separate price list, NOT an FX conversion (off-peak Flash miss CNY1.5 vs USD0.22 x 7.494 = CNY1.65) — do not cross-derive.
- 2026-08-14 — GOTCHA (high value, Gemini): ai.google.dev/gemini-api/docs/pricing renders model sections as collapsed accordions/tabbed panels. body.innerText returns only ~20,322 of 126,421 chars and OMITS EVERY PRICING TABLE; innerText searches on table elements return FALSE ZEROS ("December 31" = 0 via innerText, 32 via textContent). All 86 tables are present via querySelectorAll. Use textContent + direct table DOM walks only. Any past innerText-based Gemini check was structurally blind — this likely explains earlier confusion about Gemini rates.
- 2026-08-14 — CHANGED: Gemini 3.6 Flash halved to $0.75/$0.075/$3.75 through 2026-12-31 (reverts 2027-01-01); NEW Gemini 3.7 Flash at identical rates; 3.5 Flash flat at $1.50/$0.15/$9.00 with no date language. Batch/Flex = 50% of standard, Priority = 1.8x (untracked dimensions). "Cyber" = 0 hits on the Gemini page now.
- 2026-08-14 — Mistral: consolidated pricing page docs.mistral.ai/inference/pricing now exists WITH a Cached input column (Medium 3.5 $1.5/$0.15/$7.5) — supersedes mistral.ai/pricing/api; cached rate upgraded derived -> official.
- 2026-08-14 — Scheduled check result: full Foundry sweep BYTE-IDENTICAL to 2026-08-12 — an 8-tuple row-level diff over all 29,405 rows returned 0 additions / 0 removals (23 productNames, 1,694 product|meter pairs). Max effectiveStartDate still 2026-08-01 (provisioned/hourly only). All 72 GPT-5.6 meters still on the single uniform 2026-07-01 tranche — 10th consecutive run, Foundry Terra 1.25x / Luna 5.0x direct. All known absences re-confirmed (no Claude/Anthropic meter, no Grok 4.4/4.5/4.6, no Qwen serverless per-token meter, nothing newer than GPT-5.6, no Astra, no Mistral cache meter, no GLM 5.3, no Kimi K2.8/K3, no MiniMax M2.7, no DeepSeek V5). Anomalies unchanged (K2.6 Thinking Inp DZ 3-region $3.00 outlier; Grok4 Fast DZ 4-region stale tranche; MAI Models 18 meters byte-identical).
- 2026-08-14 — Direct pages re-verified unchanged: OpenAI (post-cut Sol/Terra/Luna exact; embeddings still NOT published on the page - 0 hits for "text-embedding"/"ada"/"0.13", re-confirming the 2026-08-07 provenance decision; Astra 0 hits; new footnote: regional/data-residency endpoints carry a 10% uplift for models released on/after 2026-03-05), Anthropic (Sonnet 5 single permanent row, cancellation language intact verbatim; new "Claude Platform on AWS" CCU section; Foundry CCU billing $0.01/CCU confirmed), xAI (Grok 4.6 exact, models page stamped 2026-08-12, no 4.7, "trillion" 0 hits so the 1.5T figure stays unofficial), Z.ai (no GLM 5.3), Kimi (no K2.8/K3.1; pricing moved to per-model slugs under platform.kimi.ai/docs/pricing/chat), MiniMax ("Permanent 50% off" live, "expir*" 0 hits), Qwen (all bands exact; qwen3.7-max end date re-confirmed on the campaign page, now titled "Qwen3.8-Max is Here").
- 2026-08-14 — Scope (untracked, deliberate): Gemini Flash-Lite/Pro/Omni/embedding/image families; Mistral Large 3 / Small 4 / Ministral 3; Mistral-hosted Z.ai GLM 5.2 ($1.4/$0.14/$4.4); Kimi K2.7 Code highspeed 2x; MiniMax Priority 1.5x; new Qwen open-weight ID qwen3.8-2.4t-a95b.
- 2026-08-15 — MAJOR/RESOLVED: the schema now models conditional and scheduled pricing (PRs #44-#47; design doc `docs/superpowers/specs/2026-08-14-rate-variants-design.md`). `PricingEntry` gains an optional `variants?: RateVariant[]`; each `RateVariant` is `{ label, conditions, inputUsd, cachedUsd, outputUsd, confidence?, cachedConfidence?, notes?, sourceNote? }`, and `RateConditions` is `{ from?, until?, utcHourWindows?, contextBand?, serviceTier? }` (all instants ISO-8601/UTC). `src/lib/rates.ts` resolves it: `matchesConditions(c, ctx)`; `resolveRate(entry, ctx)` — the rate in force plus which variant produced it (`variant`, `label`); `applicableVariants(entry, ctx)` — everything that applies ignoring time of day (e.g. both halves of a peak/off-peak pair, for showing the whole picture); `rateRange(entry, ctx)` — min/max input+output plus `varies`, for a "$0.66-$1.32" display; `nextRateChange(entry, ctx)` — `{ at, label }`, the next boundary and what takes over, for the countdown UI. Cardinal design rule (same spirit as the file-level `providers.ts` rule): variants store EXPLICITLY PUBLISHED numbers, never a runtime multiplier — "off-peak is half of peak" still gets its arithmetic done by a human against the source at authoring time, and every variant keeps its own `confidence`/`cachedConfidence`/`sourceNote` (falling back to the row's when omitted) so it still traces to an official page. Rendering (PR #46) surfaces the currently-effective rate, which variant is active, a countdown to the next change, and every other variant's numbers; the compare page (PR #47) prices models under a chosen scenario (time of day / prompt size / service tier).
- 2026-08-15 — Matching semantics a future editor must know (`src/lib/rates.ts`, pinned by `src/lib/rates.test.ts`): variants are evaluated in ARRAY ORDER, FIRST match wins — authoring order is priority order, put the more specific variant first (concretely: DeepSeek's `Peak` variant is listed before `Off-peak`; `Off-peak`'s conditions are just `{ from }` with no hour window, so it would win every hour if checked first). `from` is inclusive, `until` is exclusive, so back-to-back variants can share an instant without overlapping; an unparseable date never matches — a variant is dropped rather than quoting a price off a typo. `utcHourWindows` are half-open `[start, end)` on `now.getUTCHours()` and MAY WRAP MIDNIGHT; equal bounds is an empty window that never matches. A variant with a `contextBand` does NOT match when `ctx.contextTokens` is undefined — never claim a band price without knowing the prompt size. A variant with no `serviceTier` matches only `"standard"`; one with a `serviceTier` matches only that exact tier. UTC accessors only, everywhere — these are absolute instants, and a local-time read would quote the wrong price for most of the world.
- 2026-08-15 — AUTHORING RULE (prevents a live-price regression): a row's flat `inputUsd`/`cachedUsd`/`outputUsd` is the BASE rate and must always be the rate that is correct TODAY; variants describe future or conditional states only. Guarded by `src/data/providers.variants.test.ts`'s `guard: every catalog row resolves to its own base rate as of today` test: it resolves every catalog row at a fixed `now` and asserts `variant: null` — i.e. no variant is active at that instant, so the flat fields are what's actually live — plus an exact match to the row's own fields. Author a variant that's already active at that instant (classic mistake: put a future list price in the base and back-date a "current price" variant to cover it) and the `variant: null` assertion fails. Gotcha: the pinned `now` is a literal (`2026-08-14T12:00:00Z` today), not live `Date.now()` — it needs bumping forward by hand as real time passes, or the guard quietly stops representing an actual "today".
- 2026-08-15 — Rows now carrying `variants` (all three reversions are AUTOMATIC — no manual edit needed on the day): DeepSeek-V4 Pro (Direct) and DeepSeek-V4 Flash (Direct) — `Peak`/`Off-peak`, `from: "2026-08-16T16:00:00Z"`, peak windows 01:00-04:00 & 06:00-10:00 UTC (see the amended 2026-08-14 entry above). Gemini 3.6 Flash and Gemini 3.7 Flash — `Standard (from 2027)`, reverting to $1.50/$0.15/$7.50, `from: "2027-01-01T00:00:00Z"`. Qwen3.7 Max (Promo) — `List price (from September)`, reverting to $2.50/$0.25/$7.50, `from: "2026-09-01T00:00:00Z"`. The previously-tracked manual action items for 2026-08-16, 2026-09-01 and 2027-01-01 are all closed by the resolver — do not re-open any of them as outstanding work.
- 2026-08-15 — Rendering/hydration: all routes keep `export const dynamic = "force-static"` deliberately — a CDN fronts Azure SWA, and DeepSeek's peak boundaries are hour-aligned, so ISR (`revalidate: 3600`) would go stale at precisely the moment the rate changes (the worst possible phase) and, behind the CDN, the first request after expiry would serve the stale page regardless. Instead the server resolves at build time (`Date.now()` captured as `buildAtMs` in `page.tsx` / `pricing-table.tsx` / `compare/page.tsx`) and renders every variant's numbers into the HTML; client components (`rate-cell.tsx`, `variant-strip.tsx`, `compare-explorer.tsx`) fall back to that same `buildAtMs` pre-mount so the first client render is byte-identical to the server HTML, then `useNow()` takes over with the visitor's real clock post-mount to move the active-variant marker and countdown. No price flicker, and the page is correct even without JS. Do NOT add `revalidate`/`force-dynamic` to fix a "stale price" complaint — the resolver already handles it client-side.
- 2026-08-15 — Not yet migrated (deliberate, future work — the design doc scoped this into phase 2, but the shipped migration (PR #45) only covered the DeepSeek/Gemini/Qwen promo cases above): context-band ladders (Qwen's 0-32K/32K-256K/256K-1M) are still separate rows per band, with the band described in `notes`/`sourceNote` prose rather than the `model` field or a `contextBand` variant; long-context rows (GPT-5.6 Sol/Terra/Luna Long Context, GPT-5.5 Long Context) are still separate rows with the band in the `model` field itself. Service tiers (`ServiceTier`: batch/flex ~50%, priority 1.8x, Moonshot's highspeed 2x) are modeled in `types.ts` and the resolver, but zero catalog rows use a `serviceTier`-scoped variant yet.
- 2026-08-15 — Ops gotcha worth keeping (surfaced during this redesign, still true): the repo has TWO workflows — `ci.yml` runs on `pull_request` (typecheck/lint/build only; no deploy step), `azure-static-web-apps.yml` runs on `push` to `main` and does the actual Oryx build + deploy. A green PR check does NOT mean the site deployed. On 2026-08-14 a merge passed CI but the deploy failed because `next/font/google` fetched IBM Plex Sans/Mono and Newsreader from fonts.gstatic.com at build time, and that fetch failed inside the Oryx build container specifically (3 retries, `NextFontError`) — neither `ci.yml` nor a local build hits that container, so both stayed green while the deploy broke. Fonts are now self-hosted via `next/font/local` from vendored `.woff2` files under `src/app/fonts/` (zero external network calls at build time) and must stay that way. Always confirm the deploy workflow (`azure-static-web-apps.yml`) went green after a merge to main, not just the PR gate.
- 2026-08-15 — Scheduled check, ZERO catalog changes. Full Foundry sweep byte-identical to 2026-08-14 (29,405 rows / 23 productNames / 1,694 product|meter pairs); max effectiveStartDate still 2026-08-01 and those 11 rows are provisioned/hourly only, so nothing per-token is staged. All GPT-5.6 meters still on the single uniform 2026-07-01 tranche (11th consecutive run — the Foundry Terra 1.25x / Luna 5.0x direct gap persists). A per-meter re-analysis verified 98 individual meter dimensions covering every tracked Foundry row: 98 MATCH, 0 mismatch. Direct pages re-verified by raw DOM for OpenAI, Anthropic, Gemini, xAI, DeepSeek, Mistral, Qwen, Z.ai, Moonshot and MiniMax — every tracked rate matches. All known absences re-confirmed (no Claude/Anthropic meter, no Grok 4.4/4.5/4.6, no Qwen serverless per-token meter, nothing newer than GPT-5.6, no Astra, no Mistral cache meter, no GLM 5.3, no Kimi K2.8/K3, no MiniMax M2.7, no DeepSeek V5).
- 2026-08-15 — GOTCHA (naming trap, refutes a third-party claim): a news report that Foundry deprecated MiniMax M2.5 pay-per-token effective 2026-08-07 is FALSE. All six `FW MiniMax M2.5 {Inp,Cache Inp,Outp} DZ Tokens` meters are live and unchanged ($0.33/$0.033/$1.32, 22/22 regions uniform) in a dump captured 2026-08-15 — a week after the claimed date. The trap: the meter is named `FW MiniMax M2.5` WITH the M, while its sibling is the M-less `FW MiniMax 3`. Case-insensitive searches for "MiniMax 2.5", "minimax2.5" and "mini max 2.5" all return 0 rows. Same class as the Mistral/"MM3.5" and Grok/"4.3" naming misses.
- 2026-08-15 — Method (applies to every future sweep): the 29,405-row dump contains 168 rows with `type: "Reservation"`; the other 29,237 are `type: "Consumption"`. Filter to Consumption before computing any majority price. Apart from 9 "Provisioned Managed..." capacity meters, every (meterName, productName, armRegionName) triple is unique, so a per-region majority is well-defined. Proven this run: aggregating across meterNames instead of grouping by the exact (meterName, productName) pair produces garbage — one pass reported Cohere's *image* meter as the Embed v4 majority and flagged the correct $0.12/M text rate as an "outlier". Always group by exact meterName.
- 2026-08-15 — New watch: Z.ai announced GLM-5.3 on 2026-08-14 (third-party coverage only; reported as the same 743B base as GLM-5.2 with gains from post-training, weights promised in ~2 weeks). It has NO per-token price on docs.z.ai/guides/overview/pricing and NO Foundry meter (the only `glm` meters remain the 5/5.1/5.2 Fireworks DZ set), so it is not catalogable. Recheck for a published rate.
- 2026-08-15 — usgov-premium pattern generalized (was logged only for K2.6): the 2-region `usgovarizona`/`usgovvirginia` minority price appears across DeepSeek native (both tiers, all 3 dimensions), native Kimi K2.5/K2.6/K2.7, Cohere Embed (both tiers) and Grok 4.1 — always a clean premium ABOVE the commercial majority (exactly 1.25x for Kimi/Grok/Cohere, ~1.20–1.24x for DeepSeek native), never a conflict with a tracked rate. Keep using the commercial `isPrimaryMeterRegion` majority.
- 2026-08-15 — Scope (untracked, deliberate): Cohere `Rerank v4` Fast/Pro meters exist on Foundry in both Global and DZ lanes. Reranking is not a chat/completion token lane and is outside the schema — do not treat the absence as a gap.
- 2026-08-15 — Grok 4.7: third-party only (Musk quoted at "3–4 weeks", i.e. early September at the earliest). Zero hits on docs.x.ai pricing/models and zero Foundry meters. Per the Grok 4.4 precedent, demand a live docs.x.ai hit with a per-token price before adding any row.
- 2026-08-15 — PENDING, act before 2026-08-16 16:00 UTC (test-guard expiry, design call not a bump): `src/data/providers.variants.test.ts` pins `now` at `2026-08-14T12:00:00Z` and asserts every catalog row resolves with `variant: null`. From 2026-08-16 16:00 UTC the two DeepSeek Direct rows ALWAYS have a variant active (the `Off-peak` variant's conditions are just `{ from }` with no hour window), so bumping that literal past that instant will FAIL the guard for those two rows. This needs a decision, not a date bump: either exempt rows whose variants have already started, or relax the assertion to "the resolved rate equals the row's base rate OR an active variant's published rate". Flagged deliberately and left unchanged — it is a design call, and the shipped site is unaffected because the resolver already handles the switch at runtime.
- 2026-08-16 — RESOLVED (the peak/off-peak transition landed): DeepSeek's peak/off-peak pricing went LIVE today at 16:00 UTC exactly as staged. Official page re-read this run confirms the published table matches the catalog's `RateVariant`s to the cent — V4 Pro off-peak $0.66/$0.022/$1.98 and peak $1.32/$0.044/$3.96; V4 Flash off-peak $0.22/$0.007/$0.66 and peak $0.44/$0.014/$1.32; peak windows 01:00-04:00 and 06:00-10:00 UTC, off-peak exactly half of peak. NO manual edit was needed — the resolver switched automatically, which is the first real-world validation of the variants design shipped in PRs #44-#47. Note the flat base fields on those two rows ($0.435/$0.003625/$0.87 and $0.14/$0.0028/$0.28) are now permanently superseded by an always-active variant; that is expected and is exactly what the PR #57 guard allowlist exists to permit.
- 2026-08-16 — CLOSED (stale item, do not chase): the 2026-08-15 note flagged a test-guard fix as "PENDING, act before 2026-08-16 16:00 UTC". It was already shipped that same day in PR #57 (commit cfd5cd4) — `src/data/providers.variants.test.ts` now keeps the strict `variant: null` assertion for all rows except a small commented allowlist of 5 rows whose variants are legitimately permanently-active (DeepSeek V4 Pro/Flash Direct, Qwen3.7 Max Promo, Gemini 3.6/3.7 Flash). Any other row resolving non-null still fails loudly. The pinned `now` was bumped to 2026-08-16T18:00:00Z this run, which exercises that allowlist against the real post-transition DeepSeek state.
- 2026-08-16 — GOTCHA (high value, repeat of a documented trap): a full-sweep agent reported "Grok 4.3 meters REMOVED from Foundry" — FALSE. It searched for the string "grok 4.3" and hit the trap already recorded on 2026-07-23: the meters are named with a BARE version (`4.3 Inp Glbl Tokens`, `4.3 Cached Inp DZ Tokens`, ...), so a 'grok'-qualified search returns zero. Verified PRESENT via `productName eq 'Azure Grok Models'` (1,000 rows / 47 meterNames / 12 Grok-4.3 meters) and a `contains(meterName,'4.3')` cross-check (391 rows): Global $1.25/$0.20/$2.50, DataZone $1.375/$0.22/$2.75, long-context "L" variant exactly 2x, eff. 2026-05-01 — all exact matches to the catalog. Two tells exposed the false claim before any edit: the same report claimed meters were removed while also reporting a byte-identical total row count (29,405), which cannot both be true; and it reported 21 distinct productNames when the actual count is 23. Sanity-check internal consistency of sweep reports before acting.
- 2026-08-16 — GOTCHA (high value, NEW research technique for the Gemini page): mapping a pricing table to its model on ai.google.dev/gemini-api/docs/pricing requires tracking H2 and H3 SEPARATELY. Each model is an H2, immediately followed by four H3 tier sub-headings (Standard / Batch / Flex / Priority), each with its own `<table>`. The obvious "nearest preceding heading" walk with a single variable is clobbered by the H3 and yields `Standard`/`Batch`/... as the table's label — never the model name — so it matches ZERO model sections and looks like the models are absent. Correct approach: `document.querySelectorAll('h1,h2,h3,h4,table')` (document order), track current-H2 and current-H3 in two variables, then filter on the H2. This returns exactly 12 sections for the three Flash models (3 models x 4 tiers). This trap is distinct from, and compounds with, the already-documented innerText blindness (use textContent only).
- 2026-08-16 — Gemini 3.5 Flash re-refuted a THIRD time: it is $1.50 / $0.15 / $9.00 FLAT with ZERO promo language ("December 31" and "January 1, 2027" absent from all four of its tier tables), verified by verbatim raw-DOM cell capture using the H2/H3 method above. A sweep agent had claimed all three Flash models share the $0.75/$0.075/$3.75 promo; it over-generalized from 3.6/3.7. Durable detail worth keeping: 3.6/3.7 Flash revert on 2027-01-01 to $1.50/$0.15/$7.50, whose OUTPUT ($7.50) still differs from 3.5 Flash's flat $9.00 — the three are genuinely distinct rate structures and never converge, so never assume the 2027 reversion makes them equal. Page stamped "Last updated 2026-08-13 UTC".
- 2026-08-16 — Scheduled check, ZERO catalog changes. Foundry: full sweep 29,405 rows / 23 productNames (verbatim list re-captured this run) / 1,688-1,694 product|meter pairs; max effectiveStartDate still 2026-08-01 and those rows are provisioned/hourly capacity meters only (`Provisioned Managed Data Zone Unit` $1.20/hr, Foundry Local `Provisioned Managed Unit` $17.167/hr), so nothing per-token is staged. All GPT-5.6 meters still on the single uniform 2026-07-01 tranche — 12th consecutive run, Foundry Terra $2.50/$0.25/$15.00 vs direct $2.00/$0.20/$12.00 (1.25x) and Luna $1.00/$0.10/$6.00 vs direct $0.20/$0.02/$1.20 (5.0x). Spot-checks exact: FW GLM 5 DZ, DeepSeek V4 Pro native DZ, Kimi K2.6 FW DZ, Mistral MM3.5 Global (still no cache meter), Cohere Embed v4 Global, Grok 4.1 Global, Grok 4.3 Global/DZ/L. MAI Models unchanged (510 rows, 18 meters, 12 cryptic letter meters byte-identical, identity still unmapped). All known absences re-confirmed: no Claude/Anthropic meter, no Grok 4.4/4.5/4.6/4.7, no Qwen serverless per-token meter, nothing newer than GPT-5.6, no Astra, no Mistral cache meter, no GLM 5.3, no Kimi K2.8/K3, no MiniMax M2.7, no DeepSeek V5.
- 2026-08-16 — Direct pages re-verified unchanged by raw DOM: Z.ai (GLM-5 $1.00/$0.20/$3.20, 5-Turbo, 5.1, 5.2 all match; GLM-5.3 announced 2026-08-14 but STILL has no per-token price — not catalogable), xAI (Grok 4.6 $2.00/$0.50/$6.00, 4.5, 4.3 all match; no Grok 4.7; models page stamped 2026-08-12), Anthropic (Sonnet 5 single permanent $2/$0.20/$10 row, Opus 5, Fable 5, Mythos 5 all match), OpenAI (post-cut Sol/Terra/Luna exact; no Astra price; nothing newer than 5.6), Qwen (qwen3.8-max $2.00/$0.20/$6.00, qwen3.7-max 50%-off and Plus 20%-off promos still live with no end-date text on the pricing page itself, flash bands and 3.6-max-preview all match), Kimi (K2.6, K3 match; no K2.8/K3.1), MiniMax (M3 "Permanent 50% off" $0.30/$0.06/$1.20 still live with no expiration language, M2.7 matches), Mistral (Medium 3.5 $1.50/$0.15/$7.50).
- 2026-08-17 — BUG FIXED (customer-visible; first real fallout of the variants design): the homepage provider cards' "from $X /M in" stat was computed by a local `fromPrice()` in `src/app/page.tsx` as `Math.min(...p.entries.map(e => e.inputUsd))` — reading FLAT BASE fields and bypassing the resolver entirely. Once DeepSeek's peak/off-peak variants became permanently active at 2026-08-16 16:00 UTC, the two DeepSeek Direct rows' base fields ($0.435 and $0.14) became unreachable, so the homepage advertised DeepSeek "from $0.14" — a rate charged at no hour. Fixed: the helper moved to `src/lib/from-price.ts` and now uses `rateRange(e, { now }).minInputUsd`. DeepSeek reads $0.19 (the V4 Flash **Global** row, which has no variants and undercuts the Direct row's cheapest reachable rate, Off-peak $0.22); no other provider's figure moved. `rateRange` was chosen over `resolveRate` DELIBERATELY: `applicableVariants` WAIVES `utcHourWindows`, so a statically-rendered "from" price cannot go stale at a peak boundary, and it still filters date windows and `serviceTier` (so Gemini's Batch $0.375 and the various priority tiers never leak into a standard-tier "from" figure). Forward-looking too: Qwen's 2026-09-01 and Gemini's 2027-01-01 reversions will now raise their from-prices automatically.
- 2026-08-17 — AUDIT RESULT worth keeping (where the resolver is and is not bypassed): `pricing-table.tsx` branches on `hasVariants` — variant rows render via `RateCells`→`resolveRate`, non-variant rows render flat fields (both correct). `compare-explorer.tsx`, `compare-sort.ts` and `rateRange` all read resolved rates. `src/app/page.tsx`'s from-price was the ONLY place aggregating flat base fields across rows. A regression guard in `src/lib/from-price.test.ts` now asserts, for EVERY provider, that the advertised from-price equals the minimum of each row's `rateRange(...).minInputUsd`, so a future always-active variant cannot silently re-introduce this class of bug.
- 2026-08-17 — STALE DOC POINTER (fix on sight): AGENTS.md's changelog rule and the daily-check task prompt both say to edit `src/data/providers.ts`, but that file NO LONGER EXISTS. The catalog is split into `src/data/providers/*.ts` (claude, deepseek, embeddings, gemini, glm, kimi, minimax, mistral, openai-azure, qwen, xai) with `src/data/providers/index.ts` assembling and re-exporting. Edit the per-provider file.
- 2026-08-17 — RESOLVED/CLOSED (DeepSeek price-increase watch, open since 2026-08-02): the vague "significant increase planned … subject to official notice" footnote is GONE from the pricing page (`increase`, `significant`, `official notice` all 0 hits). The increase it foreshadowed WAS the peak/off-peak transition, which has now landed and is officially dated: the changelog gained a 2026-08-13 "DeepSeek-V4-Pro Update" entry stating verbatim that the new prices "take effect at 16:00 (UTC Time) on August 16, 2026", off-peak at half of peak. The page now carries only two footnotes (peak/off-peak, and a concurrency-limits pointer). Published table matches the catalog's variants to the cent: V4 Pro off-peak $0.66/$0.022/$1.98 & peak $1.32/$0.044/$3.96; V4 Flash off-peak $0.22/$0.007/$0.66 & peak $0.44/$0.014/$1.32. Version strings unchanged (V4-Pro-0813, V4-Flash-0731); still no V5. Reminder: the CNY table is a separate price list, NOT FX (V4-Pro off-peak cache-miss ¥4.5 vs $0.66 x 7.494 = ¥4.95) — do not cross-derive.
- 2026-08-17 — GLM-5.3 watch ESCALATED (still not catalogable): docs.z.ai/guides/overview/pricing lists GLM-5.3 in the sidebar tagged "New" but in NO pricing table, and its dedicated page /guides/llm/glm-5.3 (JSON-LD dateModified 2026-08-17) states verbatim "The GLM-5.3 API will be available soon. Stay tuned for updates." alongside "GLM-5.3 is now available to all GLM Coding Plan users". That page has ZERO `$` characters and zero "pric" hits — subscription-only today, per-token API explicitly pending. It also confirms the third-party report: "It uses the same base model as GLM-5.2, with all improvements driven by post-training." Expect a rate within days.
- 2026-08-17 — Scheduled check, ZERO rate changes. Foundry: full 29,405-row sweep (29,237 Consumption / 168 Reservation; 23 productNames, list byte-identical to 2026-08-16; 1,694 product|meter pairs); 42 tracked Foundry rows machine-checked across 116 dimensions, 0 mismatches. ZERO rows with effectiveStartDate >= 2026-08-02; the max remains 2026-08-01 and those 11 rows are provisioned/hourly capacity meters only (`Provisioned Managed Data Zone Unit` $1.20/hr across the 9 APAC DZ regions; Foundry Local `Provisioned Managed Unit` $17.167/hr). No new model launched on Foundry, and no meter yet carries the post-2026-09-01 premium shape (no EU-DZ-at-1.20x meter exists). All known absences re-confirmed with productName-scoped evidence: no Claude/Anthropic meter (0 hits for `claude|anthropic|opus|sonnet|haiku|mythos|fable` across meterName, productName, skuName AND armSkuName), no Grok 4.4/4.5/4.6/4.7, no Qwen serverless per-token meter (`Qwen models` = 8 meterNames, all Qwen3-32B fine-tuning hosting), nothing newer than GPT-5.6 (version tokens exactly {5, 5.1, 5.2, 5.3, 5.4, 5.5, 5.6}), no Astra, no gpt-5.6-cyber meter, no Mistral cache meter (43 meterNames, 0 match `cach|cd |ch inp`), no GLM 5.3, no Kimi K2.8/K3/K3.1, no MiniMax M2.7, no DeepSeek V5.
- 2026-08-17 — GPT-5.6 Foundry/direct gap: 13th consecutive run, unmoved. All 72 sol/terra/luna meters carry exactly ONE distinct effectiveStartDate (2026-07-01) — no second tranche. Foundry Terra $2.50/$0.25/$15.00 = 1.25x direct ($2.00/$0.20/$12.00); Luna $1.00/$0.10/$6.00 = 5.0x direct ($0.20/$0.02/$1.20); Sol at parity ($5/$0.50/$30). BOTH sides re-verified fresh this run (retail API + developers.openai.com raw DOM). The DZ lane is again confirmed as 13 US/EU regions with ZERO APAC overlap, so this family is a uniform 1.10x and the APAC 1.20x premium does not touch it.
- 2026-08-17 — Anomalies unchanged: (i) native `K2.6 Thinking Inp DZ` $3.00/M in exactly northeurope/switzerlandnorth/switzerlandwest vs the 16-region $1.045/M majority (2.87x) — the matching output meter is uniform $5.00, so only the input dimension is broken; gov minority $1.306/M. (ii) `Grok4 Fast` DZone 4-region stale tranche ($0.48/$1.90 in polandcentral/switzerlandnorth/switzerlandwest/uksouth vs $0.22/$0.55 across 18). (iii) MAI Models still 510 rows / 18 meters, the 12 cryptic letter meters byte-identical — 6 Global/DZ pairs at exactly 1.10x ($1.75→$1.925 twice, $5.00→$5.50, $8.00→$8.80, $19.50→$21.45, $47.00→$51.70), `skuName` still a bare `MAI <letter>`, identity STILL unmapped. Notably NO `MAI-Thinking-1` or `MAI-Code-1-Flash` named meter appeared, despite aggregators now quoting $2/$8 for MAI-Thinking-1 — third-party only, and it matches no letter pair, so do not map letters to models on that basis.
- 2026-08-17 — GOTCHA (OpenAI pricing page is a 4-pane tier switcher): developers.openai.com/api/docs/pricing renders FOUR identically-headed "Flagship models" tables as `data-content-switcher` panes — `standard`, `batch`, `flex`, `fast` — and only pane 0 (`standard`) is visible; the other three carry `hidden`. A naive "first/last table" read silently returns Batch (50%) or Fast mode (2x). The tracked lane is pane 0. Also confirmed there: cache-write is 1.25x input throughout, Batch = Flex = 50% of standard, Fast mode = 2x, and a footnote records that "Priority processing was renamed Fast mode on July 30, 2026".
- 2026-08-17 — Source-coverage note (no catalog impact): OpenAI's pricing page has DROPPED GPT-5.5/5.4/5.2/5.1 entirely — it now covers only the 5.6 family, `chat-latest` ($5.00/$0.50/$30.00), `gpt-5.3-codex` ($1.75/$0.175/$14.00; Fast mode 2x), and o4-mini finetuning. Every GPT-5.5/5.2 row in `src/data/providers/openai-azure.ts` is a Foundry row (Global/DataZone) sourced from the Azure retail API — that file has ZERO `tier: "Direct"` rows — so no tracked row lost its source. Also newly seen: `daybreak-blue-latest` and `daybreak-red-latest` are moving aliases onto gpt-5.6-sol and gpt-5.6-cyber whose price follows whatever they later resolve to — never catalog an alias.
- 2026-08-17 — GOTCHA (stale page stamp): docs.x.ai/developers/pricing reads "Last updated: July 3, 2026" while listing Grok 4.6 (released Aug 12) — that stamp is UNRELIABLE, do not use it to conclude "no change since July". The models page and release-notes both stamp 2026-08-12, and the release-notes entry independently quotes Grok 4.6's rates verbatim ("$2 / $0.50 / $6 per 1M tokens … below 200k prompt tokens, and $4 / $1 / $12 above"). `4.7` and `trillion` remain 0 hits across all three xAI pages, so the third-party 1.5T parameter figure stays unofficial.
- 2026-08-17 — GOTCHA (MiniMax tab panels): platform.minimax.io/docs/guides/pricing-paygo renders M3's rates in TWO tab panels distinguishable only by container id/aria — `panel-standard-0` (aria-label `standard`) and `panel-priority-1` (aria-label `priority`), fronted by "Standard" / "Priority*" buttons. The tracked lane is Standard; reading the Priority panel yields the 1.5x rates and looks like a 50% price rise. "Permanent 50% off" appears 4x verbatim while `expir` and `limited` are 0 hits — a fourth independent refutation of the lapsed-launch-promo claim.
- 2026-08-17 — GOTCHA (Kimi pages are client-rendered): platform.kimi.ai pricing slugs must be NAVIGATED, not fetched — a same-origin `fetch()` returns 200 with ~227KB of HTML but ZERO tables. Its column order is also Cache Hit BEFORE Cache Miss, easy to invert positionally. `/docs/pricing` redirects to `/docs/pricing/chat`, and the sidebar is a complete 5-page inventory (K3, K2.7 Code, K2.6, K2.5, Moonshot V1) — no K2.8/K3.1. New: the Moonshot V1 Aug-31 sunset language has DISAPPEARED from /docs/pricing/chat-v1 (`sunset`, `august 31`, `deprecat` all 0 hits); no catalog row references V1, but do not read that absence as a page-structure failure.
- 2026-08-17 — Qwen extraction recipe still valid, widget counts drifting: `div.tab-item` is now 236 (was 233 on 08-11, 62 on 08-07); 64 tabs read exactly "Singapore"; clicking all of them brings visible tables to 86 of 246. All 9 tracked rows MATCH. The 2026-08-31 promo end date is STILL absent from the pricing page itself (`august 31`, `until`, `expir`, `end date` all 0 hits; `Limited-time` appears 145x but never with a date) — fourth consecutive run confirming it lives only on alibabacloud.com/en/campaign/qwen-discount. qwen3.6-max-preview's 2026-10-10 deprecation is likewise absent here (0 hits) and lives only on the decommissioning page. 253 distinct model IDs visible; no qwen3.9 or qwen4.
- 2026-08-17 — NEW scope observation (untracked, deliberate): Alibaba Model Studio now RESELLS third-party models at its own prices — `glm-5.2-fast-preview` ($2.800/$8.800), `kimi-k2.7-code` ($0.95/$4), `MiniMax-M2.5` ($0.304/$1.213), `deepseek-v4-pro-0813`. These resale lanes are priced DIFFERENTLY from the vendors' own direct APIs, so never use a Model Studio row to cross-check a vendor's direct rate. Also newly priced there: `qwen3-coder-next` ($0.3/$1.5 ≤32K, $0.5/$2.5 ≤128K, $0.8/$4 ≤256K); and `qwen3.8-2.4t-a95b` open-weight prices identically to qwen3.8-max ($2/$6).
- 2026-08-17 — Gemini 3.5 Flash refuted a FOURTH time, now with per-section hit counts: within its entire section (all four tier tables, 3,110 chars) `December 31`, `January 1, 2027`, `through` and `starting` are ALL 0, while the 3.6 and 3.7 sections return 16 each. 3.5 Flash is flat $1.50/$0.15/$9.00. Page stamped "Last updated 2026-08-13 UTC"; the documented H2/H3 double-tracking walk returned exactly 12 sections (3 models x 4 tiers) as expected. Minor upstream inconsistency, no catalog impact: 3.5 Flash's cached rate is written $0.075 in its Batch table but $0.08 in its Flex table.
- 2026-08-17 — RESOLVED/REVERSED (supersedes the 2026-07-29 Cohere delisting conclusion): cohere.com/pricing DOES publish a per-token Embed 4 rate — `Cost` `$0.12 / 1M tokens` and `Image cost` `$0.47 / 1M tokens`, read from live rendered DOM after clicking the "Advanced retrieval models" tab (tab strip: Workplace systems / Generative models / Advanced retrieval models / Coding models). The group's CTA is "Get your API key" and Model Vault is a SEPARATE section with its own hourly/monthly table, so this is the standard pay-as-you-go lane, not dedicated-instance-only. The $0.47 image rate independently matches Azure's `Embed v4 Img` meter, and docs.cohere.com/docs/how-does-cohere-pricing-work states embeddings are "priced based on the number of tokens embedded" and points to cohere.com/pricing as the source of truth. The Direct row is upgraded `derived` -> `official`. Embed 4's card has NO output and NO cached dimension — a single `Cost` plus a separate `Image cost`.
- 2026-08-17 — GOTCHA (high value, retires an Internet-Archive heuristic): the 2026-07-29 finding that Cohere "delisted" per-token Embed pricing was STRUCTURALLY BLIND. Wayback captures server-rendered HTML only, and on cohere.com/pricing only the DEFAULT "Workplace systems" tab is server-rendered — the other three tabs' cards are client-only, so `0.12`, `0.47` and `Image cost` are all absent from the SSR HTML even though the page publishes them. The rate vanishing from snapshots between 2025-07-16 and 2025-08-09 is at least as consistent with a tabbed redesign as with a delisting. NEVER conclude "delisted" from archive snapshots of a client-rendered pricing page. Related environment gotcha: this page uses React 19 paint-aligned Suspense reveal (`$RB`/`$RV`), and in a non-compositing browser pane (`document.visibilityState === "hidden"`, `requestAnimationFrame` never fires) the reveal never flushes and `main` stays empty — that is a pane artifact, not a broken page. Also seen and harmless: six `rel=preload` script chunks 404 from Cohere's own CDN on uncached HTML, with no ChunkLoadError and hydration still completing — do not read those 404s as "the pricing page is broken".
- 2026-08-18 — Scheduled check, ZERO catalog changes. Fresh full Foundry sweep byte-identical to 2026-08-17 (29,405 rows: 29,237 Consumption / 168 Reservation; 23 productNames; 1,694 product|meter pairs, same per-product row counts). Max effectiveStartDate still 2026-08-01 (11 provisioned/hourly capacity meters only: `Provisioned Managed Data Zone Unit` $1.20/hr across the 9 APAC DZ regions, Foundry Local `Provisioned Managed Unit` $17.167/hr) — nothing per-token staged, and no meter anywhere yet carries the post-2026-09-01 EU-DZ 1.20x shape (scanned all 511 DZ-ish meters for a second commercial price group; every 1.091x split found is the known APAC set, never an EU region). All 72 GPT-5.6 sol/terra/luna meters still on the single uniform 2026-07-01 tranche — 14th consecutive run, Foundry Terra 1.25x / Luna 5.0x direct persists (Terra $2.50/$0.25/$15.00 Foundry vs $2.00/$0.20/$12.00 direct; Luna $1.00/$0.10/$6.00 vs $0.20/$0.02/$1.20). Spot-checks exact across FW GLM 5/5.1/5.2 DZ, FW MiniMax 3 DZ, FW MiniMax M2.5 DZ, DeepSeek V4 Pro (native + Fireworks DZ), Kimi K2.5/K2.6/K2.7 Code (native + Fireworks DZ), Mistral MM3.5, Cohere Embed v4, OpenAI embeddings, Grok 4.1, Grok 4.3 (incl. 2x long-context). All known absences re-confirmed with productName-scoped evidence: no Claude/Anthropic meter, no Grok 4.4/4.5/4.6/4.7 (Grok product tops out at 4.3; the Grok4.5 regex hits are GPT-4.5 legacy rows under `Azure OpenAI`, not Grok), no Qwen serverless per-token meter, nothing newer than GPT-5.6, no Astra, no gpt-5.6-cyber meter, no Mistral cache meter, no GLM 5.3, no Kimi K2.8/K3/K3.1, no MiniMax M2.7 meter, no DeepSeek V5. Known anomalies unchanged: native `K2.6 Thinking Inp DZ` 3-region $3.00 outlier vs $1.045 majority; `Grok4 Fast` DZone 4-region stale 2025-09-01 tranche; MAI Models still 510 rows/18 meters, 12 cryptic letter meters byte-identical, identity still unmapped. Two new untracked-model DZone outliers spotted (no catalog impact): `Code Fast 1 Inp DZone` polandcentral $0.32 vs $0.22 majority, and `Grok-4 Inp/Outp DZone` polandcentral $6.05/$30.30 vs $3.30/$16.50 majority — same stale-tranche shape as the known Grok4 Fast anomaly, on models the catalog doesn't track.
- 2026-08-18 — Direct pages re-verified unchanged by raw DOM (all 11 tracked providers): Z.ai (GLM-5/5.1/5.2/5-Turbo exact; GLM-5.3's dedicated page `dateModified` JSON-LD is byte-identical to 2026-08-17's stamp and still has 0 `$` characters and 0 "pric" hits — untouched for 24h, still not catalogable), xAI (Grok 4.6/4.5/4.3 exact; "4.7" and "trillion" still 0 hits across pricing/models/release-notes; newest release-notes entry is Aug 12), Anthropic (Sonnet 5 single permanent row unchanged, both 1.1x statements verbatim), OpenAI (post-cut Sol/Terra/Luna and gpt-5.6-cyber exact; "Astra"/"5.7"/"gpt-6" all 0 hits), DeepSeek (peak/off-peak table matches the catalog's shipped RateVariants to the cent on all 12 figures; newest changelog entry still 2026-08-13; "V5" 0 hits), Gemini (3.6/3.7 Flash promo and 3.5 Flash flat rate both exact; 3.5 Flash refuted as promo-priced a FIFTH time via per-section hit counts, all 0), Qwen (all 9 tracked rows exact; qwen3.7-max's Aug-31-2026 end date re-confirmed verbatim on the campaign page, still absent from the pricing page itself; no qwen3.9/qwen4), Kimi (K3/K2.6/K2.7 Code incl. Highspeed variant, K2.5 all exact; "k2.8"/"k3.1" 0 hits), MiniMax (M3 "Permanent 50% off" and M2.7 exact; "expir"/"limited" still 0 hits), Mistral (Medium 3.5 exact, cached column still present), Cohere (Embed 4 $0.12/$0.47 exact, single-dimension card confirmed structurally via the page's own RSC payload).
- 2026-08-18 — Tooling gotchas found this run (no catalog impact): (1) OpenAI's pricing-page tier switcher changed its DOM shape — the previously-documented `[data-content-switcher]` selector now returns 0 matches; panes are children of `div#content-switcher-latest-pricing` (siblings exist for image/video/specialized/ft pricing) with hidden panes carrying a bare `hidden` attribute instead. The safe recipe is now: enumerate all `<table>` elements and compute visibility by walking ancestors for `hidden`/`display:none`, not by selecting on the old attribute name. (2) Cohere's pricing page tab panel would not hydrate in a non-compositing browser pane (`document.visibilityState === "hidden"` blocks React's paint-aligned Suspense reveal, so `main.textContent` stays empty even after a programmatic `.click()`); the reliable fallback is reading the page's own embedded RSC flight payload from its `<script>` tags (searchable after unescaping `\\"` / `\"`) — it's first-party data straight from the origin, just a different read path than a rendered DOM dump. (3) Minor: the Moonshot V1 Aug-31 sunset language, previously noted as gone from `/docs/pricing/chat-v1`, is still present as the V1 card's subtitle on the Kimi pricing overview page — location moved, not removed; no catalog row references V1 either way.
- 2026-08-18 — Gotcha (news-sweep contamination, same class as prior LiteLLM/Q&A false claims): a WebSearch-only news sweep surfaced two claims worth flagging as refuted rather than repeating uncritically — a Microsoft Q&A-style claim that Azure "synced" the OpenAI GPT-5.6 Terra/Luna direct-API cuts, and a claim that Foundry deprecated `MiniMax-M2.5`/`GLM-5.1` pay-per-token serverless on 2026-08-07. Today's full Foundry retail-API sweep refutes both directly: all 72 GPT-5.6 meters remain on the single 2026-07-01 tranche (no second tranche exists at all), and `FW MiniMax M2.5` and `FW GLM 5.1` DZ meters are both fully live and unchanged. Treat generic web search results on Foundry pricing as leads to verify against the retail API, never as confirmation on their own.
- 2026-08-19 — RESOLVED (closes the GLM-5.3 watch escalated 2026-08-17): GLM-5.3 crossed from "API available soon" to a published per-token rate on docs.z.ai/guides/overview/pricing. Verbatim table row: `["GLM-5.3", "$1.4", "$0.26", "Limited-time Free", "$4.4"]` — $1.4 input / $0.26 cached / $4.4 output per 1M USD, identical to GLM-5.1 and GLM-5.2. "Limited-time Free" is in the Cached Input Storage column only, per the documented four-column trap. 1M-token context, 128K max output, reasoning ALWAYS enabled with three effort levels (low/high/max) — disabling is not supported. The "available soon"/"stay tuned" language is now 0 hits on /guides/llm/glm-5.3. Both pages' JSON-LD dateModified is 2026-08-18T17:07:47Z (a shared site-build timestamp, so it dates the build, not necessarily the price row). NO Foundry meter — Direct-lane-only row added this run. Note the third-party outlets (VentureBeat et al.) that reported $1.4/$4.4 ahead of this run turned out correct, but the row was only added after a first-party raw-DOM read.
- 2026-08-19 — Scheduled check, otherwise ZERO changes. Foundry: full paged sweep 29,405 rows (29,237 Consumption / 168 Reservation), 23 productNames, 1,694 product|meter pairs, 1,673 distinct meterNames — identical totals to 08-17/08-18. Of 40 non-Direct catalog rows, 36 have meters and were machine-checked across 95 dimensions: 95 MATCH, 0 mismatch. The 4 unmeterable rows are by design (Claude Sonnet 5 Global = CCU billing, no Anthropic meter; the three Gemini Flash Global rows are sourced from Google's own page — Gemini has no Foundry presence). Max effectiveStartDate still 2026-08-01 (11 hourly capacity rows only: `Provisioned Managed Data Zone Unit` $1.20/hr across the 9 APAC DZ regions, Foundry Local `Provisioned Managed Unit` $17.167/hr); zero rows >= 2026-08-02. GPT-5.6 gap persists — 15th consecutive run, all 72 sol/terra/luna meters on ONE effectiveStartDate (2026-07-01), Foundry Terra $2.50/$0.25/$15.00 = 1.25x direct and Luna $1.00/$0.10/$6.00 = 5.00x direct, Sol at parity; DZ lane is 13 US/EU regions with zero APAC overlap. All known absences re-confirmed across meterName/productName/skuName/armSkuName/serviceFamily/meterId: no Claude/Anthropic meter (0 hits, all 6 fields), no Grok 4.4/4.5/4.6/4.7 (Azure Grok Models version tokens = {1, 3, 4, 4.1, 4.2, 4.3}; the `4.5` regex hits are `gpt 4.5 0227` legacy rows), no Qwen serverless per-token meter, nothing newer than GPT-5.6, no Astra, no gpt-5.6-cyber, no Mistral cache meter, no GLM 5.3, no Kimi K2.8/K3/K3.1, no MiniMax M2.7, no DeepSeek V5. Known anomalies all unchanged (K2.6 Thinking Inp DZ 3-region $3.00 outlier; Grok4 Fast / Grok-4 / Code Fast 1 polandcentral+Swiss+uksouth stale tranches; northeurope rounded outliers; MAI Models 510 rows / 18 meters, 12 cryptic letter meters byte-identical, identity still unmapped).
- 2026-08-19 — NEW method finding (Sept-1 premium check, now systematic): to test whether any meter yet carries the post-2026-09-01 EU-Data-Zone 1.20x shape, pair each DZ meter with its Global sibling and histogram EU-price ÷ Global-majority. This run: 339 pairs with EU regions, 317 groups at exactly 1.1000, ZERO at 1.20x; all 13 non-1.10x groups are already-documented artifacts (sub-cent rounding on Grok-3 Mini / DeepSeek V4 Flash / V3.1 / gpt-4.1-nano; the 1.25x K2.6-and-K2.7-Code output oddity; the stale polandcentral/Swiss tranches; the 3.16x K2.6 Thinking input anomaly). For contrast the APAC 1.20x premium is present and unchanged — 30 price groups across the 5.1/5.2/5.3-codex/5.4/5.5 DZ meters, always exactly 9 regions. This ratio-histogram is the reusable test for "has a Sept-1-shaped meter landed yet".
- 2026-08-19 — Method gap worth closing: the "nothing new on Foundry" conclusion currently rests on identical totals at four levels (rows, Consumption/Reservation split, productNames, product|meter pairs) plus zero meters newer than 2026-07-01 outside the 11 hourly rows — NOT on a row-level diff, because no prior dump is persisted between runs. A simultaneous add-and-remove netting to identical counts at all four levels would not be caught. Persisting one dump between runs would close this permanently.
- 2026-08-19 — Tooling drift (OpenAI pricing page, 2nd change in 3 days): `div#content-switcher-latest-pricing` now has three children (`content-switcher-selector`, `content-switcher-panes`, one unnamed) rather than the documented flat pane list; the older `[data-content-switcher]` selector was already dead. The durable recipe that survives both changes: enumerate every `<table>` and compute visibility by walking ancestors for `hidden`/`display:none` — do not select on any switcher-specific attribute or child shape.
- 2026-08-19 — Untracked sightings, deliberate (new this run): Alibaba Model Studio now prices open-weight `qwen3.8-27b` at $0.5/$3 International (alongside the known `qwen3.8-2.4t-a95b` at $2/$6); Gemini has Omni Flash Preview ($1.50 in / $9.00 text out) and Gemini 3.5 Live Translate; Mistral Leanstral 1.5 (free); Anthropic publishes an agent "Session runtime" SKU at $0.08 per session-hour; Cohere Rerank 4 Fast/Pro are per-1K-searches ($2 / $2.50). All outside the tracked per-token chat lanes.
- 2026-08-19 — Re-verified unchanged by raw DOM this run (all 11 direct providers): OpenAI (post-cut Sol $5/$0.50/$30, Terra $2.00/$0.20/$12.00, Luna $0.20/$0.02/$1.20; Astra/5.7/gpt-6 all 0 hits; `text-embedding` still 0 hits, re-confirming the 2026-08-07 provenance decision), Anthropic (Sonnet 5 single permanent row, cancellation sentence verbatim, both 1.1x statements present plus a third Foundry-specific instance), Gemini (3.6/3.7 Flash promo + 2027 reversion across all four tiers; 3.5 Flash FLAT $1.50/$0.15/$9.00 — sixth refutation, neither "through December 31, 2026" nor "starting January 1, 2027" appears in any of its four tier tables; the 3.5-Flash Batch-$0.075-vs-Flex-$0.08 cached inconsistency persists), xAI (Grok 4.6 and 4.5 cross-confirmed against the page's own embedded `globalThis.__XAI_PUBLIC_MODELS__` JSON in units of $1e-4/M; complete inventory is exactly 7 model entries, no 4.7, `trillion` still 0 hits), DeepSeek (all 12 peak/off-peak figures match the shipped RateVariants to the cent; changelog newest still 2026-08-13; no V5), Mistral (Medium 3.5 $1.5/$0.15/$7.5), Cohere (Embed 4 $0.12 + image $0.47 read from the page's own RSC flight payload, single-Cost-plus-Image cost shape confirmed structurally), Qwen (all 7 tracked rows; both promos live; the Aug-31 end date re-confirmed verbatim ONLY on alibabacloud.com/en/campaign/qwen-discount — "runs until August 31, 2026" — and still 0 hits on the pricing page itself, 5th consecutive run; Plus's 20% off still has no published end date anywhere; no qwen3.9/qwen4 among 260 model IDs), Kimi (K3, K2.7 Code + Highspeed, K2.6, K2.5 all match; sidebar inventory is exactly 5 pages, no K2.8/K3.1), MiniMax (M3 Standard $0.30/$0.06/$1.20 with "Permanent 50% off" 8x verbatim and `expir`/`limited` 0 hits — 5th confirmation; Priority panel 1.5x; M2.7 and M2.5 legacy all match).
- 2026-08-20 — MAJOR/RESOLVED (closes the GPT-5.6 Foundry/direct gap, open since 2026-08-04, unmoved for 16 consecutive runs): Foundry SYNCED the Terra and Luna cuts. A meter tranche effective **2026-08-01** REPLACES the old rates outright — after this run's full sweep, ZERO terra/luna meters carry the 2026-07-01 tranche any more (only `sol` still does, unchanged). New Global rates: Terra ShortCo $2.00/$0.20/$12.00 with Cd Wr $2.50; Terra LongCo $4.00/$0.40/$18.00 with Cd Wr $5.00; Luna ShortCo $0.20/$0.02/$1.20 with Cd Wr $0.25; Luna LongCo $0.40/$0.04/$1.80 with Cd Wr $0.50 — every figure an exact match to OpenAI's direct page read via raw DOM the same day. DataZone followed at a clean 1.10x (Terra $2.20/$0.22/$13.20, Luna $0.22/$0.022/$1.32, still 13 US/EU regions, still zero APAC). PP (priority) meters were cut alongside and remain exactly 2x Standard (Terra $4.00/$0.40/$24.00, Luna $0.40/$0.04/$2.40); still ShortCo-only, no LongCo PP. Catalog updated via this PR (4 rows + 2 priority variants + intro + quirk). Retrospective on sourcing: the 2026-08-08 Microsoft Q&A claim that Azure had synced "effective 2026-08-01" — logged then as refuted — turns out to have been RIGHT about the date and merely ~11 days ahead of the meters actually appearing. The retail API was still correct at the time it was queried; the lesson is that a Q&A claim is a lead worth re-checking on a schedule, not a permanent refutation.
- 2026-08-20 — MAJOR/FIRST SIGHTING (Sept-1 EU premium): the post-2026-09-01 EU-Data-Zone 1.20x shape has LANDED on a real meter, ahead of the date. `chat-latest 08062026 opt Dz 1M Tokens` (eff. 2026-08-01) is a two-price DZ meter: $33.00/M across 7 US regions (centralus, eastus, eastus2, northcentralus, southcentralus, westus, westus3) = 1.10x the $30.00 Global, and **$36.00/M across 6 EU regions** (francecentral, germanywestcentral, polandcentral, spaincentral, swedencentral, westeurope) = **1.20x**. This is the first EU-at-1.20x group ever seen — the 2026-08-19 ratio-histogram run found 317/339 pairs at exactly 1.1000 and zero at 1.20x. Consistent with the grandfather clause (a NEW gpt-5-chat-latest snapshot, not an existing model), so no tracked row changes. Caveat worth carrying: only the `opt` (output) meter of this snapshot exists — there is no matching `inp` or `cd inp` meter yet, so the listing is partial. The openai-azure Data Zone quirk now documents this.
- 2026-08-20 — MAJOR/RESOLVED (closes the MAI Models identity mystery, open since 2026-07-27): MAI models now have NAMED meters with published per-token rates, eff. 2026-08-01. `MAI-Thinking-1` Global $2.00 in / $0.20 cached / $8.00 out per M (28-32 commercial regions; usgov at the usual 1.25x). `MAI-Cyber-1-Flash` Global $0.60 / $0.06 / $3.50. Both Global-only — no DataZone lane. The $2/$8 figure independently matches what aggregators had been quoting since 2026-08-17, and microsoft.ai publishes model pages for both. IMPORTANT residual: the 12 cryptic `MAI o`–`MAI z` letter meters are STILL present, still 2026-06-01, still the same prices, still unmapped — and their region coverage COLLAPSED this run, from 32 Global / 19 DZ down to 1-6 regions each. So the letters were never Thinking-1/Cyber-1-Flash placeholders; they look like a separate, shrinking set. Keep that sub-item open. Also new and out of scope: `MAI Image 2.5 Pro` (text in $5/M, image in $8/M, image out $106/M). NOT added to the catalog — MAI would be a brand-new provider with no direct-API baseline, which is a scope call for the owner, not a daily-check call.
- 2026-08-20 — NEW/STRONG watch (Kimi K3 on Foundry, deliberately NOT catalogued): `Azure Kimi` gained six meters eff. 2026-08-01 named `Model 6 Outp DZ/glbl` and `Model 7 Inp DZ/glbl`, `Model 7 Outp DZ/glbl`, plus a `Provisioned Managed Global Unit`. Their six values are {$3.00, $3.30, $0.30, $0.33, $15.00, $16.50} per M — EXACTLY Kimi K3's direct rate ($3.00 in / $0.30 cached / $15.00 out, re-read from platform.kimi.ai/docs/pricing/chat-k3 this run) plus its clean 1.10x DataZone counterpart. But the dimension labels are demonstrably SCRAMBLED: `Model 7 Inp glbl` carries $3.30 (the DZ input), `Model 6 Outp DZ` carries $3.00 (the Global input), `Model 7 Outp glbl` carries $0.33, and no `Cd Inp` meter name exists at all. So the price SET matches K3 perfectly while no individual meter can be trusted to name its own dimension. No row added — assigning those numbers to dimensions would mean importing the mapping from Moonshot's page and presenting it as an Azure rate, which is the exact fabrication trap this log keeps catching. Recheck for renamed/corrected meters.
- 2026-08-20 — NEW watch (DeepSeek V4 Flash version split on Foundry): a full `V4 Flash 0731` meter set landed eff. 2026-08-01 — Global $0.44 in / $0.014 cached / $1.32 out, DZ $0.484/$0.015/$1.452 — alongside the still-live plain `V4 Flash` meters ($0.19/$0.028/$0.51 Global) that the catalog's row tracks. Two things make this worth a scope decision rather than a silent edit: (a) the 0731 Global rate is EXACTLY DeepSeek's direct **peak** rate ($0.44/$0.014/$1.32), i.e. Foundry appears to bill the new snapshot flat at what DeepSeek charges only during peak hours; and (b) on the direct side `deepseek-v4-flash` now RESOLVES to `DeepSeek-V4-Flash-0731`, so the catalog's Foundry "V4 Flash" row and its Direct row are arguably no longer the same model version. Left unchanged this run; flagged for the owner. All other tracked DeepSeek rows re-verified exact (V4 Pro Global $1.74/$0.145/$3.48, V4 Pro DZ $1.91/$0.16/$3.83, FW DZ $1.925/$0.165/$3.828).
- 2026-08-20 — Sweep totals CHANGED for the first time since 2026-08-14: 30,305 rows (was a byte-identical 29,405 on every run 08-14 through 08-19), 30,131 Consumption / 174 Reservation, still 23 productNames, 1,729 product|meter pairs (was 1,694), 1,710 distinct meterNames. The 2026-08-01 tranche went from 11 rows (provisioned/hourly only) to 2,050 rows / 127 price groups. Max effectiveStartDate is STILL 2026-08-01 — zero rows >= 2026-08-02. Note this vindicates the 2026-08-19 "method gap" entry: identical totals across four levels had been the only evidence of "nothing new", and this run shows what a real change looks like by comparison. Persisting one dump between runs would still be worth doing.
- 2026-08-20 — Re-verified UNCHANGED this run, machine-checked against the fresh dump: Kimi K2.5/K2.6/K2.7 Code Global and FW K2.6 DZ; DeepSeek V4 Pro/Flash (native + Fireworks); FW GLM 5/5.1/5.2 DZ; Grok 4.3 Global/DZ and Grok 4.1 Global; Mistral MM3.5 Global ($1.50/$7.50) and DZ ($1.65/$8.25); FW MiniMax M2.5 and MiniMax 3 DZ; Cohere Embed v4 Global/DZ. Direct pages re-read via raw DOM: OpenAI (Sol/Terra/Luna/cyber/codex/chat-latest all exact; `astra`, `5.7`, `gpt-6`, `text-embedding` all 0 hits), xAI (Grok 4.6 $2/$0.50/$6, 4.5, 4.3 exact; `4.7`, `grok 5`, `trillion` all 0 hits), Anthropic (Sonnet 5 $2/$0.20/$10, Opus 5, Fable 5, Mythos 5 exact; both 1.1x statements still present), Z.ai (GLM-5/5-Turbo/5.1/5.2/5.3 exact; no GLM-5.4/6), DeepSeek (all 12 peak/off-peak figures match the shipped RateVariants to the cent; no V5), Kimi (K3 $3.00/$0.30/$15.00; sidebar still exactly 5 pages, no K2.8/K3.1). All known Foundry absences re-confirmed across meterName/productName/skuName/armSkuName/serviceFamily: no Claude/Anthropic meter, no Grok 4.4/4.5/4.6/4.7 (Azure Grok Models still tops out at 4.3), no Qwen serverless per-token meter (8 Qwen3-32B fine-tuning SKUs only), nothing newer than GPT-5.6, no Astra, no gpt-5.6-cyber meter, no Mistral cache meter (0 of 43 meterNames match `cach|cd |ch inp`), no GLM 5.3, no MiniMax M2.7, no DeepSeek V5.
- 2026-08-21 — MAJOR/RESOLVED (closes the "no Kimi K3 on Foundry" absence, re-confirmed ~15 consecutive runs since 2026-07-29): `FW Kimi K3` Data Zone meters LANDED, eff. 2026-08-01 — `FW Kimi K3 Inp DZ Tokens` $0.0033/1K ($3.30/M), `FW Kimi K3 Cd Inp DZ Tokens` $0.00033/1K ($0.33/M), `FW Kimi K3 Opt DZ Tokens` $0.0165/1K ($16.50/M), one uniform price group across all 20 commercial DZ regions, no usgov row, no rounding outlier. meterName AND skuName agree exactly on every dimension, so unlike the native lane the labels are trustworthy. Exactly 1.10x Moonshot's direct $3.00/$0.30/$15.00 (re-read same day via raw DOM at platform.kimi.ai/docs/pricing/chat-k3). Catalog row added this run (kimi.ts, Fireworks-hosted DataZone). NOTE the armSkuName values are junk here — `FW Kimi K3 Inp DZ` carries arm `Model 14 Inp glbl`, `Cd Inp DZ` carries `Model 15 Inp DZ`, `Opt DZ` carries `Model 12 Inp glbl` — same class as the 2026-07-28 "Grok 6" armSkuName non-leak; use meterName/skuName.
- 2026-08-21 — Native `Azure Kimi` K3 meters STILL scrambled (watch narrowed, NOT closed; opened 2026-08-20): the six `Model 6`/`Model 7` meters are byte-identical to yesterday, but REGION COUNT now separates the two lanes cleanly — the 35-region group is {$3.00 (`Model 6 Outp DZ`), $0.30 (`Model 7 Inp DZ`), $15.00 (`Model 6 Outp glbl`)} = the Global lane, and the 19-region group is {$3.30 (`Model 7 Inp glbl`), $0.33 (`Model 7 Outp glbl`), $16.50 (`Model 7 Outp DZ`)} = the Data Zone lane, which matches the new FW meters to the cent. So the SET is confirmed twice over, but no individual meter names its own dimension and there is still no `Cd Inp`-named meter at all. Deliberately did NOT add a Foundry Global K3 row: assigning $3.00/$0.30/$15.00 to in/cached/out would mean importing the mapping from Moonshot's page and presenting it as an Azure rate. Documented as a `warn` quirk on the Kimi provider page. Recheck for renamed meters — a Global row becomes addable the moment they are fixed.
- 2026-08-21 — SCOPE CALL FOR THE OWNER (verified, deliberately NOT added): `FW Deepseek-v4-Flash` Data Zone meters are NEW this run (eff. 2026-08-01, exclusively that tranche, 20 regions uniform) at $0.15/M in / $0.03/M cached / $0.31/M out — CHEAPER than the tracked native V4 Flash **Global** row ($0.19/$0.028/$0.51), which would make it DeepSeek's cheapest lane anywhere and move the homepage from-price from $0.19 to $0.15. Held back because adding it invites a consistency question the daily check should not settle alone: the native `V4 Flash Inp/cached/Outp DZ` meters ($0.21/$0.031/$0.56) also exist and have NEVER been catalogued, so DeepSeek V4 Flash currently has two un-tracked Foundry DZ lanes, only one of which is new. Decide whether to add both, one, or neither. (The whole native `Azure Deepseek Models` V4 Pro/Flash meter set was RE-TRANCHED to 2026-08-01 this run with every price unchanged — do not misread that re-dating as new listings.)
- 2026-08-21 — Other new Fireworks DZ listings, untracked by deliberate scope: `FW GLM 5.2 Fast` $2.31/$0.231/$7.26 (= 1.1x $2.10/$0.21/$6.60; this is the `glm-5.2-fast-preview` Alibaba resells, not a Z.ai-priced 5.x lane), `FW Inkling` $1.10/$0.19/$4.46 (model identity UNKNOWN — no vendor name in the meter and no match to any tracked family; new watch item), `FW NVIDIA Nemotron 3.5 LN` $0.06/$0.01/$0.22 and `FW NVIDIA Nemotron 3U` $0.66/$0.13/$2.64 (NVIDIA is not a tracked provider). All eff. 2026-08-01, all 20-region DZ-only, same shape as the rest of the Fireworks family.
- 2026-08-21 — Sept-1 EU premium spread (extends the 2026-08-20 first sighting): `chat-latest 08062026` now has its FULL meter set, not just the `opt` meter the 08-20 note flagged as partial — `inp Dz` and `cd inp Dz` landed too, and all three carry the same two-price shape (US 7 regions at 1.10x = $5.50/$0.55/$33.00, EU 6 regions at **1.20x** = $6.00/$0.60/$36.00). Its Global siblings are $5.00/$0.50/$30.00 across 25 regions. Still consistent with the grandfather clause (a new gpt-5-chat-latest snapshot), still zero tracked rows affected, and still the ONLY EU-at-1.20x group in the dataset. Note the sibling `chat-latest 07012026` set is single-priced DZ at $5.50/$0.55/$33.00 across 13 regions — the pre-Sept-1 shape — so the two snapshots sit side by side as a clean before/after.
- 2026-08-21 — Sweep totals: 30,833 rows (was 30,305 on 08-20, 29,405 on 08-14 through 08-19), 30,663 Consumption / 170 Reservation, still 23 productNames (list unchanged), 1,746 product|meter pairs (was 1,729), 1,727 distinct meterNames. Max effectiveStartDate is STILL 2026-08-01 — zero rows >= 2026-08-02 — but that tranche now holds 2,724 rows (was 2,050), so "max date unchanged" is no longer even weak evidence of "nothing new"; 119 meters now exist ONLY in the 2026-08-01 tranche and enumerating those is the reliable what's-new test in the absence of a persisted prior dump.
- 2026-08-21 — Verification: 66 individual meter dimensions covering every tracked Foundry row machine-checked against the fresh dump — 66 MATCH, 0 mismatch (GPT-5.6 sol/terra/luna Global + DZ + LongCo + PP; GPT-5.5/5.3/5.2 Global and DZ; embeddings 3-large/3-small/ada Global; Grok 4.3 Global/DZ/long-context-L and Grok 4.1 Global; Kimi K2.5/K2.6/K2.7 Code Global and FW K2.6 DZ; DeepSeek V4 Pro/Flash Global, V4 Pro native DZ and FW DZ; FW GLM 5/5.1/5.2 DZ; FW MiniMax M2.5 and MiniMax 3 DZ; Mistral MM3.5 Global/DZ; Cohere Embed v4 Global/DZ). Reminder on meter names that bit earlier runs: Grok 4.3's DataZone meters are `4.3 Inp DZ Tokens` (NOT `DZone` — the `DZone` suffix belongs to the Grok 4.1 / Grok-4 / Code Fast 1 meters), and the OpenAI embedding Global meters are `text-embedding-3-large-glbl` / `-small-glbl` / `embedding-ada-glbl` under productName `Azure OpenAI`.
- 2026-08-21 — All known Foundry absences re-confirmed across meterName/productName/skuName/armSkuName/serviceFamily: no Claude/Anthropic meter (0 hits for claude|anthropic|opus|sonnet|haiku|mythos|fable — Sonnet 5 Foundry rows stay `estimate`), no Grok 4.4/4.5/4.6/4.7 (Azure Grok Models tops out at 4.3), no Qwen serverless per-token meter (`Qwen models` = the same 8 Qwen3-32B fine-tuning hosting SKUs), nothing newer than GPT-5.6 (version tokens exactly {5.1, 5.2, 5.3, 5.4, 5.5, 5.6}), no Astra, no gpt-5.6-cyber meter (the 6 `cyber` hits are MAI-Cyber-1-Flash), no Mistral cache meter (0 of 43 meterNames), no GLM 5.3, no Kimi K2.8/K3.1, no MiniMax M2.7, no DeepSeek V5.
- 2026-08-21 — Direct pages re-verified UNCHANGED by raw DOM: OpenAI (Sol $5/$0.50/$30 + LC $10/$1/$45, Terra $2/$0.20/$12 + LC $4/$0.40/$18, Luna $0.20/$0.02/$1.20 + LC $0.40/$0.04/$1.80, cyber $12.50/$1.25/$75; `astra`, `5.7`, `gpt-6`, `text-embedding` all 0 hits — the 2026-08-07 embeddings provenance decision still stands), xAI (Grok 4.6/4.5/4.3 exact, cross-confirmed against the page's own `__XAI_PUBLIC_MODELS__` JSON; `4.7`, `grok 5`, `trillion` all 0), Anthropic (Sonnet 5 single permanent $2/$2.50/$4/$0.20/$10 row, Opus 5, Opus 4.8, Fable 5, Mythos 5 all exact; `inference_geo` 18 hits and `1.1x` 16 hits; no Sonnet 5.1 / Opus 5.1), Z.ai (GLM-5 $1/$0.2/$3.2, 5-Turbo, 5.1, 5.2, 5.3 all $1.4/$0.26/$4.4; no GLM-5.4/GLM-6), DeepSeek (all 12 peak/off-peak figures match the shipped RateVariants to the cent; `increase` 0 hits, no V5), Gemini (3.7 and 3.6 Flash $0.75/$0.075/$3.75 through 2026-12-31 then $1.50/$0.15/$7.50; 3.5 Flash FLAT $1.50/$0.15/$9.00 — SEVENTH refutation, its Standard table carries no date language at all; no Gemini 3.8), Mistral (Medium 3.5 $1.5/$0.15/$7.5, cached column present), MiniMax (M3 Standard $0.30/$0.06/$1.20 with "Permanent 50% off" 8x and `expir`/`limited` both 0 hits — sixth confirmation; M2.7 $0.3/$0.06/$1.2), Qwen (all 7 tracked rows exact; 3.7 Max 50%-off and 3.7 Plus 20%-off promos still live, `august 31` still 0 hits on the pricing page itself — 6th consecutive run confirming the end date lives only on alibabacloud.com/en/campaign/qwen-discount; no qwen3.9/qwen4). Cohere was NOT re-read this run (verified 08-18/08-19; single-dimension row, no movement in months).
- 2026-08-21 — Scope sightings, untracked (new this run): DeepSeek added `deepseek-v4-flash-vision-exp` to its pricing table at rates identical to V4 Flash on every dimension (experimental vision variant, outside the tracked text lane); MAI Models gained `Image 2.5 Pro` text/image meters and its named `MAI-Thinking-1` ($2.00/$0.20/$8.00 Global) and `MAI-Cyber-1-Flash` ($0.60/$0.06/$3.50 Global) lanes are unchanged from 08-20 (MAI is still a new-provider scope call for the owner, not a daily-check call); a new `Azure Kimi | Provisioned Managed Global Unit` at $1.00/hr; and `embedding-ada-glbl-new` ($0.10/M, 20 regions) which duplicates the tracked ada rate exactly.
- 2026-08-22 — MAJOR/NEW GAP (mirror image of the Terra/Luna story, which had only just closed): OpenAI cut **GPT-5.6 Sol** on 2026-08-21 — direct now $4.00/$0.40/$20.00 short context and $8.00/$0.80/$30.00 long context (cache write $5.00/$10.00), down from $5.00/$0.50/$30.00 and $10.00/$1.00/$45.00. Verified by raw DOM on developers.openai.com/api/docs/pricing with the tier-pane self-check: the three hidden panes read exactly 50% (Batch), 50% (Flex) and 200% (Fast mode) of the visible pane, which proves pane 0 is Standard and rules out the documented pane-confusion trap. Cross-confirmed by the page's own "Cyber models" table, which lists gpt-5.6-sol at the same figures. VERBATIM and important: "GPT-5.6 Sol's promotional pricing is available at least through November 21, 2026" — promotional, open-ended ("at least"), with NO published reversion rate, so do NOT author a RateVariant for it (variants require an explicitly published number). Foundry has NOT followed: all sol meters still carry the single 2026-07-01 tranche at $5.00/$0.50/$30.00, so Foundry Sol is now 1.25x direct on input/cached and 1.50x on output (DataZone stacks 1.10x on top). Terra and Luna remain at exact parity. Catalog rates unchanged (the Foundry meter is what bills); the Sol rows' notes/sourceNotes, the provider tagline/intro and the parity quirk were rewritten. WATCH: for a 5.6-sol meter tranche dated 2026-09-01 or later, and for the promo's fate after 2026-11-21.
- 2026-08-22 — Scope note the owner may want to revisit: `src/data/providers/openai-azure.ts` still has ZERO `tier: "Direct"` rows (see 2026-08-17), so the Sol cut cannot be shown as a rate anywhere on the site — only as prose in the row notes and the quirk. Every other provider has a Direct comparison lane. Adding an OpenAI Direct lane is a scope call, not a daily-check call, but the Sol gap is the first time the absence actually costs the reader a number.
- 2026-08-22 — RESOLVED (closes the DeepSeek V4 Flash Foundry Data Zone scope call left open 2026-08-21): BOTH lanes added, on the V4 Pro precedent (which has tracked a native + a Fireworks DZ row side by side since 2026-07-27). Native `V4 Flash Inp/cached/Outp DZ Tokens` $0.21/$0.031/$0.56 across 22 commercial regions — the Global rate x1.10 rounded UP on all three dimensions ($0.19 to $0.209 to $0.21). Fireworks `FW Deepseek-v4-Flash In/Cd In/Opt DZ Tokens` $0.15/$0.03/$0.31, eff. 2026-08-01, uniform across 20 regions, no usgov or northeurope outlier. NOTE the consequence: the Fireworks DZ lane is BELOW the Global tier on every dimension, so DeepSeek's homepage from-price moved $0.19 to $0.15 and `src/lib/from-price.test.ts`'s two DeepSeek literals were updated (the structural regression guard passed untouched, which is the point of it). A Data-Zone-cheaper-than-Global inversion is possible whenever two different hosts sell the same model — tier labels do not order prices across hosts.
- 2026-08-22 — Left deliberately untracked: `V4 Flash 0731` has a full native meter set (Global $0.44/$0.014/$1.32, DZ $0.484/$0.015/$1.452, eff. 2026-08-01) whose Global rate is exactly DeepSeek's direct PEAK rate. Adding it means adding a snapshot-specific row, and there is a live version mismatch to think about first: DeepSeek direct's `deepseek-v4-flash` now resolves to `DeepSeek-V4-Flash-0731`, while the catalog's Foundry V4 Flash rows track the plain `V4 Flash` meters — arguably an older snapshot. Recorded in the new rows' sourceNotes; still an owner scope call.
- 2026-08-22 — MAJOR/RESOLVED (closes the MAI letter-meter mystery, open since 2026-07-27 and carried through ~20 runs): the cryptic `MAI o`-`MAI z` meterNames are GONE. Microsoft renamed them; the letters survive only as `armSkuName`, which now maps each one to a real product name. They were never text LLM meters — they are IMAGE generation meters, and the mapping reproduces the six price pairs recorded on 2026-08-06 exactly: s/y = Image 2.5 Flash Text Input ($1.75/$1.925), t/z = Image 2.5 Flash Image Input ($1.75/$1.925), p/v = Image 2.5 Text Input ($5.00/$5.50), q/w = Image 2.5 Image Input ($8.00/$8.80), u/o = Image 2.5 Flash Image Output ($19.50/$21.45), r/x = Image 2.5 Image Output ($47.00/$51.70). The 2026-08-06 temptation to read $1.75 as GPT-5.2 input pricing was a coincidence, and the standing refusal to map letters to text models was correct. Out of schema scope; the watch is CLOSED. Durable lesson: when a meterName is a placeholder, check `armSkuName` on a LATER run for the rename rather than trying to infer identity from price.
- 2026-08-22 — Sept-1 EU premium, second and third sightings (extends 2026-08-20/08-21, which had only `chat-latest 08062026`): `gpt-transcribe Dz Unit` and `gpt-live-transcribe Dz Unit` (both new, eff. 2026-08-01) carry the same two-price shape — 7 US regions at 1.10x and 6 EU regions (francecentral, germanywestcentral, polandcentral, spaincentral, swedencentral, westeurope) at exactly 1.20x, against Global $0.27 and $1.02 per unit. Audio, so out of schema scope, but it confirms the pattern is systematic for newly-launched models rather than a one-off on the chat-latest snapshot. The ratio histogram this run: 52 multi-price DZ meters, and the ONLY 1.20x EU groups are those three families; every other 1.20x group is the known 9-region APAC set, and the rest are the documented stale/rounding artifacts (Grok-4 and Code Fast 1 polandcentral, Grok4 Fast 4-region, K2.6 Thinking 3-region, northeurope rounding).
- 2026-08-22 — Sweep totals: 30,833 rows (30,663 Consumption / 170 Reservation) — the row TOTAL is byte-identical to 2026-08-21, but product|meter pairs moved 1,746 to 1,752 and distinct meterNames 1,727 to 1,730. So an identical row count is now demonstrably NOT evidence of an unchanged catalog: the MAI rename swapped names without changing row counts. Max effectiveStartDate is still 2026-08-01 (2,724 rows in that tranche; 119 meters exist ONLY there). This is the exact failure mode the 2026-08-19 "method gap" entry predicted — persisting one dump between runs remains the only real fix.
- 2026-08-22 — Verified unchanged this run. Foundry: every tracked row matched its live meter (Kimi K2.5/K2.6/K2.7 Code Global + FW K2.6/K3 DZ; DeepSeek V4 Pro Global/native DZ/FW DZ and V4 Flash Global; FW GLM 5/5.1/5.2 DZ; FW MiniMax M2.5 and MiniMax 3 DZ; Mistral MM3.5 Global/DZ; Cohere Embed v4 Global/DZ; OpenAI embeddings Global; Grok 4.3 Global/DZ/L and Grok 4.1 Global; GPT-5.6 all variants, GPT-5.5, 5.3, 5.2). Direct pages re-read by raw DOM: Anthropic (Opus 5 $5/$0.50/$25, Sonnet 5 $2/$0.20/$10, Fable 5 and Mythos 5 $10/$1/$50 — all exact), xAI (Grok 4.6 $2.00/$0.50/$6.00, 4.5, 4.3 exact via the page's `__XAI_PUBLIC_MODELS__` JSON; `4.7` and `grok 5` both 0 hits), Z.ai (GLM-5.3 stable at $1.4/$0.26/$4.4 three days after being added; no GLM-5.4/6), DeepSeek (all 12 peak/off-peak figures match the shipped RateVariants to the cent; no V5), Kimi (K3 $3.00/$0.30/$15.00; sidebar still exactly 5 pages, no K2.8/K3.1), Gemini (3.7/3.6 Flash promo + 2027 reversion exact; 3.5 Flash FLAT $1.50/$0.15/$9.00 — EIGHTH refutation, zero date language in any of its four tier tables), Mistral (Medium 3.5 $1.5/$0.15/$7.5), MiniMax (M3 Standard $0.30/$0.06/$1.20, "Permanent 50% off" 8x, `expir` 0 hits). Known absences all re-confirmed: no Claude/Anthropic meter, no Grok 4.4/4.5/4.6/4.7, no Qwen serverless per-token meter, nothing newer than GPT-5.6, no Astra, no gpt-5.6-cyber meter, no Mistral cache meter, no GLM 5.3, no Kimi K2.8/K3.1, no MiniMax M2.7, no DeepSeek V5.
- 2026-08-22 — Native `Azure Kimi` K3 meters STILL scrambled (watch unchanged, 3rd run): the six `Model 6`/`Model 7` meters are byte-identical to 2026-08-21 — the 35-region group {$3.00 `Model 6 Outp DZ`, $0.30 `Model 7 Inp DZ`, $15.00 `Model 6 Outp glbl`} is the Global lane, the 19-region group {$3.30 `Model 7 Inp glbl`, $0.33 `Model 7 Outp glbl`, $16.50 `Model 7 Outp DZ`} is the Data Zone lane. No meter names its own dimension, no `Cd Inp` name exists, so no Foundry Global K3 row is claimed. Given the MAI rename resolved the same way, expect these to be renamed rather than repriced — recheck `armSkuName` and meterName together.
- 2026-08-22 — Qwen: the pricing page itself was NOT re-read via the full Singapore-tab recipe this run (yesterday's raw-DOM pass verified all 7 rows exactly and nothing signalled a change). The campaign page WAS re-read and the Qwen3.7-Max 50%-off end date is unchanged and verbatim in two places — "The promotion is effective until August 31, 2026" and an FAQ "runs until August 31, 2026". No action needed on 2026-09-01: the row's `List price (from September)` variant reverts it automatically. Qwen3.7-Plus's 20% off is still never mentioned on the campaign page and still has no published end date anywhere.
